const fs=require('fs'),crypto=require('crypto'),path=require('path');const fail=m=>{throw Error(m)},sha=b=>crypto.createHash('sha256').update(b).digest('hex'),jcs=v=>v===null?'null':Array.isArray(v)?'['+v.map(jcs).join(',')+']':typeof v==='object'?'{'+Object.keys(v).sort().map(k=>JSON.stringify(k)+':'+jcs(v[k])).join(',')+'}':JSON.stringify(v),domain=(d,o)=>sha(Buffer.concat([Buffer.from(d+'\0'),Buffer.from(jcs(o))])),ts=(s,n)=>{if(typeof s!=='string'||!/^\d{4}-(0[1-9]|1[0-2])-([0-2]\d|3[01])T([01]\d|2[0-3]):[0-5]\d:[0-5]\dZ$/.test(s)||!Number.isFinite(Date.parse(s))||new Date(Date.parse(s)).toISOString()!==s.replace('Z','.000Z'))fail('timestamp:'+n);return Date.parse(s)},safe=p=>{const root=fs.realpathSync('.');let q='.';for(const x of p.split('/')){q=path.join(q,x);const st=fs.lstatSync(q);if(st.isSymbolicLink())fail('symlink:'+p)}if(fs.realpathSync(p)!==root+'/'+p||!fs.statSync(p).isFile())fail('unsafe:'+p);return fs.readFileSync(p,'utf8')},parse=p=>{const raw=safe(p),o=JSON.parse(raw);if(raw!==jcs(o))fail('noncanonical:'+p);return o},verifyEnvelope=(env,trust,expectedError)=>{try{if(env.recordType!=='CheckpointReceiptEnvelopeV1'||env.schemaVersion!=='1')fail('ENVELOPE_SHAPE');const cd=domain('horseness.checkpoint-receipt-core.v1',env.core);if(cd!==env.coreDigest||env.signature.signedDigest!==cd)fail('CORE_DIGEST_MISMATCH');const pre=Object.fromEntries(Object.entries(env).filter(([k])=>k!=='envelopeDigest'));if(domain('horseness.checkpoint-receipt-envelope.v1',pre)!==env.envelopeDigest)fail('ENVELOPE_DIGEST_MISMATCH');if(new Set(trust.keys.map(x=>x.keyId)).size!==trust.keys.length)fail('TRUST_KEY_DUPLICATE');const k=trust.keys.find(x=>x.keyId===env.signature.keyId);if(!k)fail('TRUST_KEY_UNKNOWN');if(k.principalId!==env.signature.principalId)fail('TRUST_PRINCIPAL_MISMATCH');const at=ts(env.core.attestedAt,'attestedAt');if(at<ts(k.notBefore,'notBefore')||at>=ts(k.notAfter,'notAfter'))fail('TRUST_KEY_TIME');if(k.revokedAt!==null&&at>=ts(k.revokedAt,'revokedAt'))fail('TRUST_KEY_REVOKED');if(!k.allowedReceiptVariants.includes(env.core.receiptVariant)||!k.allowedSubjects.includes(env.core.subjectId))fail('TRUST_SCOPE');const der=Buffer.from(k.publicKeySpkiBase64,'base64');if('sha256:'+sha(der)!==k.spkiFingerprint||!crypto.verify(null,Buffer.from('horseness.checkpoint-receipt-signature.v1\0'+cd),crypto.createPublicKey({key:der,format:'der',type:'spki'}),Buffer.from(env.signature.signatureBase64,'base64')))fail('SIGNATURE_INVALID');if(expectedError)fail('expected '+expectedError);return true}catch(e){if(expectedError&&e.message===expectedError)return false;throw e}},index=(p,d,g)=>{const raw=safe(p);if(!raw.endsWith('\n'))fail('index-newline:'+p);let prior=null;return raw.trimEnd().split('\n').map((line,i)=>{const o=JSON.parse(line);if(line!==jcs(o)||o.ordinal!==i||o.priorRecordHash!==prior)fail('index-chain:'+p);const pre=Object.fromEntries(Object.entries(o).filter(([k])=>k!=='recordHash'));if(domain(d,pre)!==o.recordHash)fail('index-hash:'+p);if(i===0&&o.recordType!==g)fail('index-genesis:'+p);prior=o.recordHash;return o})};
const owned=['AGENTS.md','README.md','docs/architecture.md','docs/plan.md','docs/progress.md','docs/security/threat-model.md','docs/compatibility.md','docs/trust-root.md','docs/progress/C00.md','docs/checkpoints/schema.json','docs/checkpoints/index.jsonl','docs/claims/index.jsonl','docs/validation/c00-contract-gate.node-e.txt','docs/validation/c01-claim-check.node-e.txt'];for(const p of owned)if(!fs.existsSync(p)||!fs.statSync(p).isFile()||!fs.statSync(p).size)fail('missing:'+p);const schema=parse('docs/checkpoints/schema.json');for(const n of ['CheckpointReceiptCoreV1','CheckpointReceiptEnvelopeV1','CheckpointSignatureV1','CheckpointTrustStoreV1','CanonicalTimestamp'])if(!schema.$defs[n])fail('schema-def:'+n);index('docs/checkpoints/index.jsonl','horseness.checkpoint-index-record.v1','CheckpointIndexGenesisV1');index('docs/claims/index.jsonl','horseness.claim-index-record.v1','ClaimIndexGenesisV1');
const base='docs/checkpoints/fixtures/c01-bundle-v1/',dig=parse(base+'digests.json');for(const [p,h] of Object.entries(dig))if(sha(safe(base+p))!==h)fail('bundle-digest:'+p);const trust=parse(base+'trust.json'),boot=parse(base+'receipts/bootstrap-v1.json'),ordinary=parse(base+'receipts/c01-ordinary-v1.json'),claim=parse(base+'claims/c01-claim-v1.json');verifyEnvelope(boot,trust);verifyEnvelope(ordinary,trust);if(parse('docs/checkpoints/fixtures/bootstrap-v1.json').envelopeDigest!==boot.envelopeDigest||parse('docs/checkpoints/fixtures/c01-ordinary-v1.json').envelopeDigest!==ordinary.envelopeDigest||parse('docs/checkpoints/fixtures/c01-claim-v1.json').claimDigest!==claim.claimDigest)fail('canonical-fixture-copy');const cpidx=index(base+'checkpoint-index.jsonl','horseness.checkpoint-index-record.v1','CheckpointIndexGenesisV1'),clidx=index(base+'claim-index.jsonl','horseness.claim-index-record.v1','ClaimIndexGenesisV1');const claimRows=clidx.filter(x=>x.claimDigest===claim.claimDigest&&x.claimPath==='claims/c01-claim-v1.json');if(cpidx.filter(x=>x.receiptDigest===boot.envelopeDigest&&x.receiptPath==='receipts/bootstrap-v1.json').length!==1||cpidx.filter(x=>x.receiptDigest===ordinary.envelopeDigest&&x.receiptPath==='receipts/c01-ordinary-v1.json').length!==1||claimRows.length!==1||claimRows[0].preClaimBaseSha!=='A00')fail('bundle-index-membership');
const pre=Object.fromEntries(Object.entries(claim).filter(([k])=>k!=='claimDigest'));if(domain('horseness.claim-attempt.v1',pre)!==claim.claimDigest||claim.preClaimBaseSha!=='A00'||claim.dependencyReceiptDigests.length!==1||claim.dependencyReceiptDigests[0]!==boot.envelopeDigest)fail('claim-binding');const graph=parse(base+'git-graph.json'),commits=new Map(graph.commits.map(x=>[x.sha,x])),ancestor=(a,b,seen=new Set())=>a===b||(!seen.has(b)&&(seen.add(b),commits.get(b).parents.some(p=>ancestor(a,p,seen))));for(const [x,y] of graph.requiredRelations)if(!ancestor(x,y))fail('fixture-ancestry:'+x+':'+y);if(graph.claimCommit.sha!=='B1'||graph.claimCommit.preClaimBaseSha!==claim.preClaimBaseSha||graph.claimCommit.tree!=='tree-k01'||commits.get('B1').parents.length!==1||commits.get('B1').parents[0]!==claim.preClaimBaseSha||commits.get('B1').tree!==graph.claimCommit.tree)fail('claim-commit-binding');const c=ordinary.core;if(c.claimIntegrationSha!=='B1'||c.workerBaseSha!=='B1'||c.claimAttemptDigest!==claim.claimDigest||!ancestor('B1','W01')||!ancestor('B1','I01')||commits.get('W01').tree!==c.candidateTree||commits.get('I01').tree!==c.candidateTree||!ancestor('I01','A01'))fail('receipt-provenance');const seal=ts(c.candidateSealedAt,'sealed'),att=ts(c.attestedAt,'attested'),exp=ts(claim.expiresAt,'expires');let last=seal;c.commandResults.forEach((r,i)=>{const s=ts(r.startedAt,'start'+i),f=ts(r.finishedAt,'finish'+i);if(r.ordinal!==i||s<seal||f<s||s<last)fail('command-chronology:'+i);last=f});if(att<last||seal>=exp||att>=exp||!c.expiryProof.sealedBeforeExpiry||!c.expiryProof.attestedBeforeExpiry)fail('receipt-chronology');
const vectors=parse(base+'signature-vectors.json'),clone=o=>JSON.parse(JSON.stringify(o));for(const v of vectors.cases){if(v.expected==='valid'){verifyEnvelope(parse(base+v.envelopePath),trust);continue}const e=clone(ordinary);if(v.name==='core-substitution')e.core.candidateTree='tree-substituted';if(v.name==='signature-substitution')e.signature.signatureBase64=boot.signature.signatureBase64;if(v.name==='unknown-key')e.signature.keyId='unknown-key';if(v.name==='revoked-key')e.signature.keyId='fixture-revoked-2026';if(v.name==='principal-substitution')e.signature.principalId='other';if(v.name!=='core-substitution'){const ep=Object.fromEntries(Object.entries(e).filter(([k])=>k!=='envelopeDigest'));e.envelopeDigest=domain('horseness.checkpoint-receipt-envelope.v1',ep)}verifyEnvelope(e,trust,v.expectedError)}
const under=parse('docs/checkpoints/fixtures/c01-claim-underscoped-negative-v1.json'),expired=parse('docs/checkpoints/fixtures/c01-claim-expired-negative-v1.json');if(under.allowedPaths.length!==1||ts(expired.expiresAt,'negative expiry')>=ts('2026-01-01T00:30:00Z','negative now'))fail('negative-fixtures');for(const [p,markers] of Object.entries({'docs/architecture.md':['CheckpointReceiptCoreV1','TaskCompletionPolicyV1','WorkerReturnV1','CommandAuthorizationMatrixV1'],'docs/plan.md':['Sole effective ordered `v3:C01` command list','C24 complete ordered v3 acceptance list','refs/heads/main'],'docs/progress.md':['**Active blockers:** none'],'docs/security/threat-model.md':['protocol-confined']}))for(const m of markers)if(!safe(p).includes(m))fail('marker:'+p+':'+m);const ledger=safe('docs/progress/C00.md');if(/^- \[ \]/m.test(ledger)||!ledger.includes('Status: `contract-frozen`'))fail('ledger');const manifest=parse('docs/checkpoints/fixtures/digests.json');if(manifest.c00ProgramSha256!==sha(safe('docs/validation/c00-contract-gate.node-e.txt'))||manifest.c01ClaimProgramSha256!==sha(safe('docs/validation/c01-claim-check.node-e.txt')))fail('program-digest');console.log('C00 contract gate passed');