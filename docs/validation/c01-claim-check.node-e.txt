const fs=require('fs'),crypto=require('crypto'),cp=require('child_process'),path=require('path');const fail=m=>{throw Error(m)},a=process.argv.slice(1),arg=n=>{const i=a.indexOf(n);if(i<0||i===a.length-1)fail('missing '+n);return a[i+1]},sha=b=>crypto.createHash('sha256').update(b).digest('hex'),jcs=v=>v===null?'null':Array.isArray(v)?'['+v.map(jcs).join(',')+']':typeof v==='object'?'{'+Object.keys(v).sort().map(k=>JSON.stringify(k)+':'+jcs(v[k])).join(',')+'}':JSON.stringify(v),domain=(d,o)=>sha(Buffer.concat([Buffer.from(d+'\0'),Buffer.from(jcs(o))])),git=(xs)=>cp.execFileSync('git',xs,{encoding:'utf8'}).trim(),ts=(s,n)=>{if(typeof s!=='string'||!/^\d{4}-(0[1-9]|1[0-2])-([0-2]\d|3[01])T([01]\d|2[0-3]):[0-5]\d:[0-5]\dZ$/.test(s)||!Number.isFinite(Date.parse(s))||new Date(Date.parse(s)).toISOString()!==s.replace('Z','.000Z'))fail('invalid canonical timestamp:'+n);return Date.parse(s)},safe=p=>{const root=fs.realpathSync('.'),parts=p.split('/');let q='.';for(const x of parts){q=path.join(q,x);const st=fs.lstatSync(q);if(st.isSymbolicLink())fail('symlink:'+p)}const r=fs.realpathSync(p);if(r!==root+'/'+p||!fs.statSync(r).isFile())fail('unsafe path:'+p);return fs.readFileSync(r,'utf8')},parseCanonical=p=>{const raw=safe(p),o=JSON.parse(raw);if(raw!==jcs(o))fail('noncanonical:'+p);return {raw,o}},index=(p,d,g)=>{const raw=safe(p);if(!raw.endsWith('\n'))fail('index newline:'+p);let prior=null;return raw.trimEnd().split('\n').map((line,i)=>{const o=JSON.parse(line);if(line!==jcs(o)||o.ordinal!==i||o.priorRecordHash!==prior)fail('index chain:'+p+':'+i);const pre=Object.fromEntries(Object.entries(o).filter(([k])=>k!=='recordHash'));if(domain(d,pre)!==o.recordHash)fail('index hash:'+p+':'+i);if(i===0&&o.recordType!==g)fail('index genesis:'+p);prior=o.recordHash;return o})},verifyEnvelope=(env,trust,at)=>{if(env.recordType!=='CheckpointReceiptEnvelopeV1'||env.schemaVersion!=='1')fail('receipt envelope');const cd=domain('horseness.checkpoint-receipt-core.v1',env.core);if(cd!==env.coreDigest||env.signature.signedDigest!==cd)fail('core digest');const ep=Object.fromEntries(Object.entries(env).filter(([k])=>k!=='envelopeDigest'));if(domain('horseness.checkpoint-receipt-envelope.v1',ep)!==env.envelopeDigest)fail('envelope digest');if(new Set(trust.keys.map(x=>x.keyId)).size!==trust.keys.length)fail('duplicate trust key');const k=trust.keys.find(x=>x.keyId===env.signature.keyId);if(!k)fail('unknown trust key');if(k.algorithm!=='Ed25519'||env.signature.algorithm!=='Ed25519'||k.principalId!==env.signature.principalId)fail('trust binding');const t=ts(at,'receipt attestedAt');if(t<ts(k.notBefore,'key notBefore')||t>=ts(k.notAfter,'key notAfter')||(k.revokedAt!==null&&t>=ts(k.revokedAt,'key revokedAt'))||!k.allowedReceiptVariants.includes(env.core.receiptVariant)||!k.allowedSubjects.includes(env.core.subjectId))fail('untrusted signer scope/time');const der=Buffer.from(k.publicKeySpkiBase64,'base64');if('sha256:'+sha(der)!==k.spkiFingerprint||!crypto.verify(null,Buffer.from('horseness.checkpoint-receipt-signature.v1\0'+cd),crypto.createPublicKey({key:der,format:'der',type:'spki'}),Buffer.from(env.signature.signatureBase64,'base64')))fail('signature invalid')};const claimPath=arg('--claim'),nowText=arg('--now'),now=ts(nowText,'trusted --now');if(claimPath!=='docs/claims/C01/1.json')fail('production claim path required');if(arg('--integrated-head')!=='HEAD')fail('integrated head must be HEAD');if(git(['symbolic-ref','--short','HEAD'])!=='main')fail('integration branch must be main');const head=git(['rev-parse','HEAD']),parents=git(['rev-list','--parents','-n','1',head]).split(' ');if(parents.length!==2)fail('K01 must have one A00 parent');const a00=parents[1],claim=parseCanonical(claimPath).o,cpi=index('docs/checkpoints/index.jsonl','horseness.checkpoint-index-record.v1','CheckpointIndexGenesisV1'),cli=index('docs/claims/index.jsonl','horseness.claim-index-record.v1','ClaimIndexGenesisV1'),a00rows=cpi.filter(x=>x.subjectId==='C00'&&x.receiptVariant==='bootstrap-v1'&&x.receiptPath==='docs/checkpoints/C00/bootstrap/0.json');if(a00rows.length!==1)fail('exact indexed A00 receipt required');const rr=a00rows[0],receipt=parseCanonical(rr.receiptPath).o,trust=parseCanonical('docs/checkpoints/trust.json').o;verifyEnvelope(receipt,trust,receipt.core.attestedAt);if(rr.receiptDigest!==receipt.envelopeDigest||rr.candidateIntegrationSha!==receipt.core.candidateIntegrationSha)fail('A00 index binding');if(git(['rev-parse',a00+'^'])!==receipt.core.candidateIntegrationSha)fail('A00 ancestry');const krows=cli.filter(x=>x.subjectId==='C01'&&x.attemptGeneration===1&&x.claimPath===claimPath);if(krows.length!==1)fail('exact indexed K01 required');const kr=krows[0],pre=Object.fromEntries(Object.entries(claim).filter(([k])=>k!=='claimDigest'));if(domain('horseness.claim-attempt.v1',pre)!==claim.claimDigest||kr.claimDigest!==claim.claimDigest||kr.preClaimBaseSha!==a00||claim.preClaimBaseSha!==a00)fail('K01 digest/base binding');const ktree=git(['rev-parse',head+'^{tree}']),claimBlob=git(['rev-parse',head+':'+claimPath]),indexBlob=git(['rev-parse',head+':docs/claims/index.jsonl']);if(git(['cat-file','-t',head])!=='commit'||!ktree||git(['cat-file','-t',claimBlob])!=='blob'||git(['cat-file','-t',indexBlob])!=='blob'||git(['show',head+':'+claimPath])!==jcs(claim)||git(['show',head+':docs/claims/index.jsonl'])+'\n'!==safe('docs/claims/index.jsonl'))fail('K01 path/tree integration');const exp=sha(Buffer.concat([Buffer.from('horseness.c01-claim.v1\0C01'),Buffer.from(a00),Buffer.from(receipt.envelopeDigest),Buffer.from(claim.expiresAt)]));if(claim.claimId!==exp||claim.dependencyReceiptDigests.length!==1||claim.dependencyReceiptDigests[0]!==receipt.envelopeDigest||claim.candidateSealedAt!==null||claim.attestedAt!==null)fail('C01 identity/dependency');const issued=ts(claim.issuedAt,'issuedAt'),expires=ts(claim.expiresAt,'expiresAt');if(!(issued<=now&&now<expires&&issued<expires))fail('claim not valid at trusted time');const ledger=git(['show',head+':docs/progress/C01.md']);if(!ledger.includes('Attempt generation: `1`')||!ledger.includes('Pre-claim base: `'+a00+'`'))fail('C01 ledger mismatch');console.log('live C01 claim valid');