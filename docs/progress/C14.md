# C14 Execution Ledger

- Status: `in-progress`
- Dependency: C13 is complete at final candidate `7cc39529` with completion commit `c4c4512a`. Exact C13 gates 1, 2, and 4 passed under the recorded Node 22 environment; exact gate 3 was explicitly waived/unobserved solely for C13 repository chunk completion, so no cross-OS receipt or validation evidence is claimed.
- Claim base: `c4c4512a`; the tracker-only C14 claim MUST be integrated before any C14 implementation edit.
- Source-owned paths: exactly `apps/cli/**`, `docs/cli.md`, the affected CLI manifest/lock files, and `.changeset/C14-cli.md`. The conventional tracker claim/completion transactions separately own only `docs/progress.md` and `docs/progress/C14.md`.
- Package-changing scope: C14 may change the `@horseness/cli` manifest and root manifest only for exact forwarding scripts required by the frozen C14 acceptance commands. `pnpm-lock.yaml` is owned when CLI manifest serialization changes and MUST be generated by pnpm. No daemon, protocol, SDK, adapter-kit, adapter, installer, bootstrap, policy, domain, orchestrator, store, CI workflow, or unrelated package source is owned by C14.
- Deliverables: implement the packed public CLI for authorized daemon lifecycle, first-authority bootstrap, credential lifecycle and recovery, and the exhaustive public coordinator command surface. The CLI MUST use the authenticated daemon protocol/SDK boundary and MUST NOT call internal orchestrator or store APIs or directly access the database.
- Typed command registry: freeze a versioned typed command registry with a public registration interface extensible without editing the router. Command definitions MUST bind command identity, aliases where explicitly supported, argument and option schema, authorization role/scope, request/result types, JSON-RPC method, output rendering, stable usage and operational errors, and secret-bearing field metadata. Duplicate names/aliases, omitted authorization mappings, unknown options, invalid workspace binding, and schema-invalid inputs fail closed.
- Command surface: expose daemon `start` and `stop`; workspace create/read; run create/read/list/status/close; task create/update-before-freeze/cancel/resolve; dependency add/list; fork create/refresh/read; context materialize/bind/read; dispatch/cancel/reconcile; artifact publish/read; receipt inspect; proposal inspect/evaluate/approve/reject/release/rebase-lineage; admission decision/history subscription and resume; canonical/history reads; joins; policy/grant/quota administration; and status. Omission from the exhaustive authorization matrix means denied.
- JSON contract: every command supports a typed machine-readable JSON mode whose successful value and stable error envelope are versioned and schema-validated. JSON output MUST be deterministic, contain no terminal decoration or mixed human prose, preserve typed cursors, idempotency inputs, pagination and subscription resume tokens, and return stable command/result/error identities. Human rendering and JSON rendering MUST derive from the same typed result rather than reparsing text.
- Credential lifecycle: implement first-authority bootstrap using the one-time owner-bound bootstrap capability; credential issuance/delegation as authorized grants; rotation that activates replacement authority before retiring prior credentials; revocation that fails closed for subsequent authentication and delegation; recovery using recovery metadata without exposing recovery-root private material; and explicit authenticated restore rebind to the restored workspace identity. Concurrent, stale, replayed, wrong-user, wrong-workspace, revoked, expired, or scope-widening credential operations MUST fail closed.
- Secret-redaction contract: secret values, bearer material, bootstrap capabilities, recovery-root private material, raw credential bytes, and sensitive environment values MUST never appear in argv echoes, help, human output, JSON output, errors, logs, diagnostics, receipts, snapshots, or test artifacts. Outputs may expose only approved opaque references, key/grant identifiers, public metadata, bounded fingerprints/digests, and redacted markers. Redaction applies on success, usage failure, daemon/protocol failure, cancellation, and partial lifecycle failure.
- CLI smoke contract: from a fresh authenticated daemon, drive exclusively through the packed CLI entry point: start, bootstrap, create workspace/run/DAG, add dependencies, create/refresh a fork, materialize and bind context, dispatch, inspect artifacts/evidence/receipt/proposal, exercise an accepted and a rejected admission loop, observe canonical/history/join/status, continue the accepted path, exercise rotate/revoke/recover and restored-workspace rebind, prove secret redaction, and stop. No direct database, test harness, internal orchestrator call, or unpacked source entry point may complete the scenario.
- Authorization and security: every mutating command carries current authorization observation cursor/context version and an idempotency input where the protocol requires it. Authority-only lifecycle, grant, policy, quota, proposal decision/release, recovery, rebind, and daemon-control operations remain authority-only; scoped roles cannot widen their grant or act across workspace/task/generation/binding boundaries. Usage, transport, authentication, stale-cursor, revocation, and authorization failures MUST remain distinguishable through stable typed errors without leaking secrets.
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; every ordered command expects exit 0. No command may be skipped, omitted, duplicated, reordered, or substituted.
- Acceptance commands (ordered):
  1. `corepack pnpm --filter @horseness/cli run typecheck`
  2. `corepack pnpm --filter @horseness/cli run test`
  3. `corepack pnpm --filter @horseness/cli run smoke`
  4. `corepack pnpm run boundaries:check`
- Completion checkpoint: `feat(cli): add authorized orchestration workflows`.
- Gate evidence: not yet run or claimed.
- Review evidence: not yet performed or claimed.
- Scheduling: C14 is active. No next chunk is dependency-ready; C15 remains ineligible until C14 completion is integrated.
- Blockers: none at claim time.

## Checklist

- [x] C13 dependency evidence recorded: final candidate `7cc39529`, completion `c4c4512a`, with the narrow gate-3 waiver represented as unobserved rather than passed
- [x] Serialized tracker-only C14 claim prepared from integrated C13 completion base `c4c4512a`
- [x] Exact C14 source ownership and package-changing scope recorded
- [x] Typed extensible command registry, exhaustive CLI surface, JSON contract, credential lifecycle, authorization, and redaction contracts recorded
- [x] Exact Node 22 environment and four ordered acceptance commands recorded
- [ ] Integrate the tracker-only C14 claim before implementation edits
- [ ] Implement the packed typed CLI and extensible command registry within exact ownership
- [ ] Implement daemon start/stop, one-time bootstrap, credential rotate/revoke/recover, and authenticated restore rebind workflows
- [ ] Implement the exhaustive authorized workspace/run/DAG/task/dependency/fork/context/dispatch/artifact/receipt/proposal/admission/history/join/status command surface
- [ ] Implement deterministic versioned JSON success/error output and comprehensive secret redaction
- [ ] Complete the fresh-daemon packed-CLI smoke with accepted and rejected loops and no internal/database shortcuts
- [ ] Run the exact acceptance commands in order under the recorded Node 22 environment
- [ ] Complete blocker/high code and security review and resolve every accepted finding
- [ ] Record the final candidate and exact gate evidence in both tracker files
- [ ] Integrate tracker completion and make C15 the sole next dependency-ready chunk
