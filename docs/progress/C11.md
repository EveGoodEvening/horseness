# C11 Execution Ledger

- Status: `in-progress`
- Dependency: C10 is complete at tracker completion commit `ec0c18e`, with final candidate `ae5a25c`; its definitive code and security reviews were clean and its ordered Node 22 gates passed.
- Claim base: `ec0c18e`; this is the serialized tracker-only claim and MUST be integrated before any C11 implementation edit.
- Source-owned paths: exactly `tests/fixtures/hosts/**`, `tests/host-feasibility/**`, `scripts/host-feasibility/**`, `config/hosts/capability-matrix.v1.json`, `docs/compatibility.md`, `docs/integrations/feasibility.md`, `package.json`, `pnpm-lock.yaml`, and `.changeset/C11-host-feasibility.md`. The conventional tracker claim/completion transactions separately own only `docs/progress.md` and `docs/progress/C11.md`.
- Package-changing expansion: C11 may change only the root `package.json` scripts and development dependencies required for the feasibility harness and `pnpm-lock.yaml`; pnpm is the sole lockfile serializer. No workspace package source or package manifest is owned by C11.
- Real native minimum: for Pi, OMP, Claude Code, and Codex, the capability matrix MUST pin the real native binary and official validator by exact supported version plus immutable distribution identity/digest. Each hermetic validator MUST prove native discovery, contribution load, context injection, one deterministic-provider attempt, receipt binding, restart/reconcile or an explicit supported-capability declaration, resume where supported, and uninstall. Missing binaries, missing official validators, unsupported required capabilities, managed blocking, or CLI-only fallback fail the applicable validation; a CLI fallback cannot satisfy C11.
- Hermetic provider: use a repository-controlled local deterministic provider with fixed request fixtures, output/evidence bytes, ordering, clock, resource budget, and provider identity. Hermetic validation MUST disable external network and credentials and MUST produce repeatable machine-readable evidence.
- Credentialed-live policy: live checks are distinct from mandatory hermetic real-binary/official-validator gates. Local/development execution may skip a live check only when its allowlisted opaque credential reference is absent, recording a stable machine-readable skip reason without reading or exposing a secret. If a live credential reference is configured, missing/invalid credentials, provenance mismatch, redaction failure, budget breach, timeout, or host/model failure is a hard failure. Publication-required live evidence MUST never skip and fails closed when unavailable.
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; every ordered command expects exit 0. No command may be skipped, omitted, duplicated, reordered, or substituted; credentialed-live skip rules do not permit any hermetic acceptance gate to skip.
- Acceptance commands (ordered):
  1. `corepack pnpm run typecheck`
  2. `corepack pnpm run boundaries:check`
  3. `corepack pnpm run host:validate:pi`
  4. `corepack pnpm run host:validate:omp`
  5. `corepack pnpm run host:validate:claude`
  6. `corepack pnpm run host:validate:codex`
  7. `corepack pnpm run host:harness:test`
  8. `corepack pnpm run hosts:matrix:verify`
- Scheduling: C11 is the sole active claim. C12 remains dependency-ineligible until C11 implementation, ordered acceptance, blocker/high review, and tracker completion are integrated.
- Blockers: none at claim time.

## Checklist

- [x] C10 completion at `ec0c18e` and final candidate `ae5a25c` recorded
- [x] Serialized tracker-only C11 claim prepared from integrated base `ec0c18e` before source edits
- [x] Exact C11 ownership and package-changing expansion recorded
- [x] Real native binary and official-validator minimums recorded for all four hosts
- [x] Hermetic deterministic-provider requirements recorded
- [x] Credentialed-live skip/fail and publication fail-closed policy recorded
- [x] Exact Node 22 environment and eight ordered gates recorded
- [ ] Tracker claim commit integrated before implementation edits
- [ ] Real binaries, official validators, capability matrix, fixtures, and hermetic harness implemented within exact ownership
- [ ] Stable host validation/smoke commands and deterministic provider implemented
- [ ] Exact acceptance commands completed in order under the recorded environment
- [ ] Blocker/high review completed and all findings resolved
- [ ] Tracker completion records final candidate/evidence and makes C12 dependency-ready
