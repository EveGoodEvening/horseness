# C15 Execution Ledger

- Status: `in-progress`
- Dependency: C14 is complete at final candidate `426ed3e6` with completion commit `c68f62a2`; all four exact C14 gates passed under the recorded Node 22 environment, and definitive code and security reviews were clean after the recorded fixes.
- Claim base: `c9a615b4`; tracker-only C15 claim `334c7b08` is integrated before C15 implementation and review-fix edits.
- Source-owned paths: exactly `adapters/pi/**`, `tests/fixtures/hosts/pi/**`, `docs/integrations/pi.md`, `docs/compatibility.md`, `.changeset/C15-pi.md`, the Pi adapter manifest and lockfile as applicable, and root `package.json` only for `scripts["host:smoke:pi"]`; narrow review-fix ownership adds exactly `packages/protocol/src/spi.ts`, `packages/adapter-kit/src/index.ts`, and `packages/adapter-kit/test/conformance.test.ts`, limited solely to preserving distinct `artifact`/`evidence` publication kinds through the existing `WorkerReturnV1` delivery path. The conventional tracker claim/completion transactions separately own only `docs/progress.md` and `docs/progress/C15.md`.
- Package-changing scope: C15 may change the `@horseness/adapter-pi` manifest and `pnpm-lock.yaml` as applicable; lockfile changes MUST be generated by pnpm. Root `package.json` ownership is limited exactly to the `scripts["host:smoke:pi"]` forwarding key, whose implementation remains adapter-owned. Shared changes may only replace the publication digest list with `{digest, kind}` and pass the kind to the existing publisher; they MUST NOT change the delivery order, binding checks, decision-scope mismatch behavior, frozen result, or any other protocol/adapter-kit behavior. C15 does not own `scripts/host-feasibility/**` or any other root manifest key. No OMP, Claude, Codex, daemon, CLI, SDK, installer, policy, domain, orchestrator, store, CI workflow, or unrelated package source is owned by C15.
- Deliverables: ship the validated Pi native Horseness bundle and adapter-owned smoke implementation. The native smoke MUST cover native load, deterministic-provider attempt, bound receipt, restart/reconcile/resume matrix, fork switch, and uninstall contribution.
- Native return contract: the real Pi native bundle MUST originate the complete `WorkerReturnV1` path through artifact/evidence publication, receipt submission, proposal sealing/submission, decision subscription/resume, and decision surfacing. It MUST prove one accepted canonical advance plus surfaced rejection, conflict, quarantine, and approval-required outcomes; harness synthesis is forbidden.
- Security contract: enforce host-native credential storage, least-privilege attempt grants, output/evidence size and media bounds, path/realpath confinement, redaction, decision-subscription resume semantics, duplicate callback idempotency, uninstall credential revocation, and explicit failure if Pi cannot provide the required native return loop.
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; every ordered command expects exit 0. No command may be skipped, omitted, duplicated, reordered, or substituted.
- Acceptance commands (ordered):
  1. `corepack pnpm --filter @horseness/adapter-pi run typecheck`
  2. `corepack pnpm --filter @horseness/adapter-pi run test`
  3. `corepack pnpm --filter @horseness/adapter-pi pack`
  4. `corepack pnpm run host:smoke:pi`
  5. `corepack pnpm run boundaries:check`
- Supplemental verification commands (after the five primary frozen gates; nonduplicated and not part of their replacement):
  1. `corepack pnpm --filter @horseness/protocol run typecheck`
  2. `corepack pnpm --filter @horseness/protocol run test`
  3. `corepack pnpm --filter @horseness/adapter-kit run typecheck`
  4. `corepack pnpm --filter @horseness/adapter-kit run test`
- Completion checkpoint: `feat(pi): ship validated native Horseness bundle`.
- Scheduling: C15 is the active claim. C16 remains blocked until C15 completion is integrated.
- Blockers: none preventing C15 execution.

## Checklist

- [x] C14 dependency evidence recorded: final candidate `426ed3e6`, completion `c68f62a2`
- [x] Dependency-ready planning-correction base `c9a615b4` recorded
- [x] Serialized tracker-only C15 claim prepared from base `c9a615b4`
- [x] Exact C15 source ownership, manifest/lock scope, root script-key restriction, tracker-only transactions, and publication-kind-only shared-path correction recorded
- [x] Exact Node 22 environment, five primary frozen acceptance commands, and four nonduplicated supplemental protocol/adapter-kit commands recorded
- [x] Integrate tracker-only C15 claim `334c7b08` before implementation edits
- [ ] Implement the validated Pi native bundle and adapter-owned `host:smoke:pi` forwarding target within exact ownership
- [ ] Prove the native load, deterministic-provider attempt, bound receipt, restart/reconcile/resume, fork-switch, uninstall, complete worker-return, and admission-outcome contracts
- [ ] Run the five exact primary acceptance commands in order, then the four supplemental verification commands, under the recorded Node 22 environment
- [ ] Complete blocker/high code and security review and resolve every accepted finding
- [x] Resolve latest C15 rereview highs: canonical output/evidence replay binding with negative native-tool proof; mandatory durable retained authority with close/reopen smoke; and two-step authority-token decision subscription whose token is checkpointed before terminal observation without republishing, resubmitting, or opening a second null-token subscription
- [ ] Record the final candidate and exact gate evidence in both tracker files
- [ ] Integrate tracker completion and make C16 the sole next dependency-ready chunk
