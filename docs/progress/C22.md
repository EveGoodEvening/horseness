# C22 Execution Ledger

- Status: `complete`; ADR 0009 and the 2026-08-16 npm-first planning correction supersede the former blocked ceremony/KMS/immutable-storage contract.
- Dependency: C21 is complete at final candidate `c47e0f70`, with tracker completion `670ea72f`.
- Existing claim: tracker-only claim `dd4bf596`; planning corrections `f97853e7` and `780940d4`; npm-first implementation `bcf7c185`; review fix and final candidate `a36619d8`. The prior C22 implementation commits `3a17c899`, `71993228`, and `b8e26497` are historical and do not satisfy the corrected contract.
- Product decision: publish a fourteen-package npm train at `1.0.0`; defer `@horseness/bootstrap`, offline media, the project trust root, KMS signing, custom immutable storage, signed side-effect journals, custom release receipts, and release-specific live receipts.
- Acceptance environment: repository root under Node 22 with `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; all nine commands exited `0` in order on candidate `a36619d8`.
- Scheduling: C22 is complete. C23 is dependency-ready but still requires the protected GitHub `release` environment and initial npm publication authority.

## Exact ownership

C22 owns the fourteen public manifests, the deferred bootstrap manifest, root release forwarding keys, canonical lockfile changes, `.github/workflows/{ci,release}.yml`, removal of `.github/workflows/{release-stage,live-gates}.yml`, the exact npm-first release scripts/tests named by the final plan correction, focused boundary logic/tests, release documentation, README status, removal of the obsolete ceremony schema and release trust scripts, and the two tracker files.

Historical C00/C01 receipts, checkpoint trust, product runtime receipts, installer state safety, fixture release/trust bytes, and completed C19–C21 evidence are read-only and unaffected.

## Sole ordered acceptance contract

1. `corepack pnpm install --frozen-lockfile`
2. `corepack pnpm run release:docs-lint`
3. `corepack pnpm run release:coherence`
4. `corepack pnpm run release:build-twice`
5. `corepack pnpm run release:verify-candidate`
6. `corepack pnpm run release:test`
7. `corepack pnpm run release:verify-commands`
8. `corepack pnpm run release:verify-no-static-secrets`
9. `corepack pnpm run boundaries:check`

## Acceptance evidence

Candidate `a36619d8b92d1c3c4c80696dce17919ace5155b8` passed the sole ordered contract on 2026-08-16:

1. Frozen pnpm install completed with the lockfile already current.
2. Release documentation linted 13 files with zero errors.
3. Coherence verified fourteen public `1.0.0` packages and deferred private `@horseness/bootstrap@0.0.0`.
4. Two independent package builds matched at inventory digest `db9e48796409bc47cac29f6140a39c16b25813a092e44f5a50d2371557715705`.
5. Candidate verification installed all packed internal dependencies, imported all fourteen packages, and exercised the installed CLI validation path.
6. Release tests passed 10/10.
7. Command verification confirmed 10 forwarding keys and the exact nine-command acceptance list.
8. Static-secret verification confirmed no retained token, fixture publication path, or obsolete trust workflow.
9. Boundary verification passed for all 15 workspace packages.

The canonical candidate manifest binds `sourceCommit` to the full candidate SHA above. Final blocker/high review was clean after source-commit binding, Windows command-shim handling, manifest/tarball symlink rejection, exact upstream-run binding, existing-tag target verification, and version-input checks were resolved. No npm package was published during C22.

## Downstream operational phases

- C23 `publish-next`: protected release environment; short-lived package-scoped npm token for initial package creation; npm provenance; exact-integrity reconciliation only.
- C24 `verify-public`: accept only the exact successful C23 run, then perform clean exact-version installation, package imports, and CLI/bin smoke on Linux, macOS, and Windows.
- C25 `promote-latest`: accept only the exact successful C23 and candidate-bound C24 runs, verify `next` and exact integrity, use maintainer 2FA or a separately provisioned short-lived promotion token, move all fourteen packages to `latest`, then create or verify the source-bound GitHub release and tag.

GitHub workflow URLs and npm registry metadata are ordinary tracker evidence. No custom release receipt, side-effect journal, or checkpoint namespace is created.

## Checklist

- [x] Record user authorization to adopt npm-first release and defer custom trust infrastructure.
- [x] Add ADR 0009 and supersede conflicting C22–C25 architecture and planning clauses.
- [x] Freeze fourteen-package scope and defer fixture-mode `@horseness/bootstrap`.
- [x] Freeze corrected C22 ownership and nine-command acceptance contract.
- [x] Replace custom release scripts and workflows with the npm-first implementation.
- [x] Return `@horseness/bootstrap` to private `0.0.0` and update the lockfile.
- [x] Run all nine acceptance commands in order.
- [x] Complete blocker/high review and record final candidate `a36619d8`.
- [x] Commit tracker completion and make C23 eligible.
