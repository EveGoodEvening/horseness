# C22 Execution Ledger

- Status: `in-progress`/`blocked`; locally reachable implementation is complete at candidate `b8e26497` with implementation/review-fix commits `3a17c899`, `71993228`, `b8e26497` and planning corrections `4d99e14e`, `02672c80`, `1568b617`, `fcf01ffb`; the ordered eleven-command acceptance contract is not green because command 1 failed with `ROOT_CEREMONY_RECORD_MISSING`, blocking commands 2–11; production trust infrastructure is not available yet.
- Dependency: C21 is complete at final candidate `c47e0f70`, with tracker completion `670ea72f`.
- Planning correction: the narrow C22–C25 release-ownership correction was independently reviewed and accepted at `4d99e14e`; it is the claim base and supersedes earlier overlapping release-path and command shorthand.
- Ownership: all fifteen publishable manifests: `packages/{domain,protocol,policy,store-sqlite,orchestrator,sdk,adapter-kit,installer}/package.json`, `apps/{daemon,cli,bootstrap}/package.json`, and `adapters/{pi,omp,claude,codex}/package.json`; `adapters/{pi,omp,claude,codex}/native-manifest.json` only when present or required by the shipped native format; `.changeset/**`; `CHANGELOG.md`; `LICENSE`; `.gitignore` solely to ignore `.acceptance/`; root `package.json` solely for C22 release, live-gate, and command-verifier forwarding keys; `pnpm-lock.yaml`; exact assembly scripts `scripts/release/{verify-root-ceremony,verify-delegation,coherence,build-twice,verify-sbom-provenance-signatures,dry-run,verify-no-static-secrets,upload-immutable,verify-artifact-receipt,verify-commands,materialize-bootstrap-trust-root}.mjs`; workflows `.github/workflows/{ci,release,release-stage,live-gates}.yml`; production bootstrap trust-root materialization paths `apps/bootstrap/generated/production-trust-root.json`, `apps/bootstrap/src/index.ts`, and `scripts/bootstrap/build.mjs`, limited solely to consuming and pinning the ceremony-derived production public trust root without changing C20 fixture behavior; `docs/{compatibility.md,migrations.md,release-process.md,trust-root.md}`; `docs/trust/root-ceremony-v1.json`; `docs/trust/root-ceremony-v1.schema.json`; `docs/trust/evidence/**`; `.acceptance/C22-artifact-receipt.json` as ignored ephemeral runtime evidence that is never committed; `docs/publication-journal/C22/**`; `docs/checkpoints/C22/side-effects/**`; and the tracker files `docs/progress.md` and `docs/progress/C22.md` for claim and completion accounting only.
- External-effect boundary: C22 uses only its append-only `docs/publication-journal/C22/**` and `docs/checkpoints/C22/side-effects/**` namespaces. KMS signing and immutable upload require integrated intent/observed pairs, lookup-first recovery, and a final ordinary receipt binding the side-effect head. Downstream chunks consume but never edit C22 evidence.
- Acceptance environment: repository root under Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; every command must exit `0`, no skip or substitution is allowed, and the exact order is mandatory.
- Scheduling: C22 is the sole active claim. C23–C25 are not-started and dependency-blocked through C22, not deferred by scope.

## Sole ordered acceptance contract

1. `corepack pnpm run release:verify-root-ceremony -- --schema docs/trust/root-ceremony-v1.schema.json --record docs/trust/root-ceremony-v1.json --evidence docs/trust/evidence --offline --threshold 2-of-2`
2. `corepack pnpm run release:verify-delegation -- --root-record docs/trust/root-ceremony-v1.json --require-version-range --require-kms-policy --require-two-approvals`
3. `corepack pnpm install --frozen-lockfile`
4. `corepack pnpm run release:coherence`
5. `corepack pnpm run release:build-twice`
6. `corepack pnpm run release:verify-sbom-provenance-signatures`
7. `corepack pnpm run live-gates:required -- --matrix config/hosts/capability-matrix.v1.json`
8. `corepack pnpm run release:dry-run`
9. `corepack pnpm run release:verify-no-static-secrets`
10. `corepack pnpm run release:upload-immutable -- --select-build build-1 --receipt-out .acceptance/C22-artifact-receipt.json`
11. `corepack pnpm run release:verify-artifact-receipt -- --checkpoint-subject C22`

Commands 1 and 2 are integrated prerequisites for every later command. No KMS signing or immutable-upload intent may be recorded before both succeed. This eleven-command sequence is the sole effective contract; no earlier nine-command row or later append instruction applies.

## External and product prerequisites

Locally reachable implementation proceeds now, so these prerequisites do not mark C22 scheduler-blocked at claim time. They remain mandatory for completion or the applicable external-effect phase and cannot be invented, fabricated, or replaced by fixture, synthetic, hermetic, stale, mismatched, or ambiguous evidence:

- approved project `LICENSE` text;
- explicit npm, native, and offline registry/destination and access decisions;
- independently reviewed offline 2-of-2 root/recovery ceremony record and evidence, including distinct keys, fingerprints, witnesses, custody, and destruction evidence;
- approved version/range-bounded release-key delegation;
- KMS key and policy with two approvals;
- protected-`main` OIDC identity for `.github/workflows/release.yml`;
- immutable retention-locked artifact storage with declared retention deadline and lookup capability;
- usable Claude and Codex existing-user subscription sessions for candidate-bound live receipts.

C23 additionally requires authenticated publication access. C24 requires fresh exact-public Claude/Codex sessions and candidate-bound Linux, macOS, and Windows receipts. C25 requires fresh successful C24 evidence and authorized promotion and announcement access.

## Bootstrap-trust path-traversal regression review-fix correction (2026-08-16)

A C22 planning review identified that the production bootstrap trust-root materialization paths owned by C22 introduce a new pre-authentication path-traversal surface at the bootstrap executable edge: a malicious signed release envelope may carry artifact paths containing `..` segments that attempt to escape the bootstrap temporary extraction root before manifest/catalog trust verification completes. The existing C20 installer operations test suite did not cover this exact pre-auth containment invariant because C20 fixture mode never exercised production trust-root materialization.

This narrow correction authorizes C22 to add only `packages/installer/test/operations/bootstrap-trust.test.ts` solely to prove that a pre-auth path-traversal envelope is rejected (exit `1`) and no escaped file is created outside the bootstrap temp root, and to cover the adjacent self-signed, wrong-identity, revoked-release, and authenticated-replay pre-auth rejections that share the same trust-verification boundary. No source path, installer implementation path, bootstrap source/distribution path, fixture path, manifest, workflow, or other test path is reopened or added. The test consumes the existing C20-built `apps/bootstrap/dist/horseness-bootstrap.mjs` executable and `apps/bootstrap/generated/fixture-release.json` envelope read-only.

Supplemental review-fix verification is `corepack pnpm --filter @horseness/installer run test` (the installer full test gate), run under Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`. This supplements rather than replaces, duplicates, reorders, or appends to the frozen eleven-command C22 acceptance contract and does not satisfy any external-effect or ceremony prerequisite. No other touched path is outside current C22 ownership.

## Boundary-metadata correction (2026-08-16)

The release plan's exact source pins are normative: although pnpm can pack `workspace:*` to exact `1.0.0`, C22 release coherence requires source manifests themselves to use exact `workspace:<coherent manifest version>` internal specifiers. C22 may therefore edit only `scripts/boundaries-check.mjs` and focused regression path `tests/boundaries-check.test.mjs` for this correction, preserving the existing private `0.0.0`/`workspace:*` development mode while requiring one coherent public release version and exact `workspace:<that version>` internal pins in release mode. No dependency-layer, import-containment, package-count, export-surface, or other boundary invariant is relaxed. Focused supplemental verification is `node --test tests/boundaries-check.test.mjs`; it does not modify the frozen eleven-command acceptance contract.

## Locally reachable implementation (2026-08-16)

Implementation proceeded within exact owned paths and is complete at candidate `b8e26497`. The implementation/review-fix commits are `3a17c899` (feat(release): enforce candidate trust assembly), `71993228` (fix(release): enforce ceremony and artifact boundaries), and `b8e26497` (feat(release): prepare public 1.0.0 packages). Planning corrections authorizing the owned-path edits are `4d99e14e` (release lifecycle ownership), `02672c80` (bootstrap pin fixes), `1568b617` (traversal regression), and `fcf01ffb` (release boundaries).

### Explicit user decisions

- Version: `1.0.0` for all fifteen publishable packages; root `package.json` remains `0.0.0` for development mode.
- License: MIT (`LICENSE` added at `b8e26497`).
- Registry: public npm access (`.changeset/config.json` `access: public`).
- Production trust infrastructure is not available yet: no offline 2-of-2 root/recovery ceremony record or evidence, no KMS key/policy, no protected-`main` OIDC identity, and no immutable retention-locked artifact storage have been provisioned.

### Implemented local tooling, metadata, tests, and reviews — complete

- Release assembly scripts: `scripts/release/{verify-root-ceremony,verify-delegation,coherence,build-twice,verify-sbom-provenance-signatures,dry-run,verify-no-static-secrets,upload-immutable,verify-artifact-receipt,verify-commands,materialize-bootstrap-trust-root,side-effect-journal,lib}.mjs` and `scripts/release/live-gates-required.mjs`.
- Release tests: `scripts/release/test/release.test.mjs` (12 cases).
- Workflows: `.github/workflows/{ci,release,release-stage,live-gates}.yml`.
- Bootstrap trust-root materialization: `scripts/release/materialize-bootstrap-trust-root.mjs`, `apps/bootstrap/src/{index,trust-pin}.ts` and dist counterparts, `scripts/bootstrap/build.mjs`.
- Bootstrap-trust path-traversal regression test: `packages/installer/test/operations/bootstrap-trust.test.ts`.
- Boundary metadata: `scripts/boundaries-check.mjs` and `tests/boundaries-check.test.mjs` for release-mode exact `workspace:1.0.0` internal pins.
- Package manifests: all fifteen publishable manifests set to `1.0.0` with `MIT` and exact internal pins; `.changeset/**` per-chunk changesets consumed/removed; `CHANGELOG.md` `1.0.0` entry; `pnpm-lock.yaml` updated.
- Root ceremony schema: `docs/trust/root-ceremony-v1.schema.json` (record and evidence directory intentionally absent — not provisioned).
- Release process docs: `docs/release-process.md`, `docs/trust-root.md`, `docs/compatibility.md`, `docs/migrations.md`.

### Supplemental green evidence (run under Node 22, `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`)

- `corepack pnpm run release:test` — 12/12 pass (exit 0).
- `node --test tests/boundaries-check.test.mjs` — 4/4 pass (exit 0).
- `node scripts/boundaries-check.mjs` — boundary check passed for 15 packages (exit 0).
- `corepack pnpm run release:coherence` — exit 0; coherent manifest graph with exact `1.0.0` internal pins.
- `node scripts/release/verify-commands.mjs` — verified 13 release forwarding keys and 11 acceptance commands (exit 0).
- `node scripts/release/verify-no-static-secrets.mjs` — verified no static secrets in C22 production release, build, workflow, and bootstrap surfaces (exit 0).
- `corepack pnpm install --frozen-lockfile` — lockfile up to date, exit 0.
- `corepack pnpm --filter @horseness/domain pack` — packed `horseness-domain-1.0.0.tgz`, exit 0 (representative pack smoke).
- `corepack pnpm --filter @horseness/installer run test` — 34/34 pass (exit 0); includes the bootstrap-trust path-traversal regression test.

### Ordered acceptance contract — not green (command 1 red, commands 2–11 blocked)

The eleven-command acceptance contract was run in order under Node 22 with exact environment. Command 1 failed; commands 2–11 are blocked by the ordered gate and by absent witnessed ceremony/evidence, root delegation/KMS/OIDC, and retention-locked storage. They are not marked green and no acceptance is claimed.

- Command 1: `corepack pnpm run release:verify-root-ceremony -- --schema docs/trust/root-ceremony-v1.schema.json --record docs/trust/root-ceremony-v1.json --evidence docs/trust/evidence --offline --threshold 2-of-2` — RED, exit 1, `ROOT_CEREMONY_RECORD_MISSING`. The record `docs/trust/root-ceremony-v1.json` and evidence directory `docs/trust/evidence` do not exist; only the schema is present. Production trust infrastructure is not provisioned.
- Commands 2–11: unrun/blocked by the ordered gate (commands 1 and 2 are integrated prerequisites for every later command) and by absent witnessed offline 2-of-2 root/recovery ceremony/evidence, root delegation/KMS policy, protected-`main` OIDC identity, and immutable retention-locked storage. No KMS signing or immutable-upload intent may be recorded before commands 1 and 2 succeed.

### Local invoking-user sessions and protected live-gate environment

Local Claude and Codex invoking-user subscription sessions exist, but the protected live-gate environment required for candidate-bound live receipts (command 7 `live-gates:required`) is not provisioned. No live-gate, cross-OS, or external-effect receipt is claimed.

### Final blocker/high locally reachable reviews

Final blocker/high reviews of the locally reachable implementation are clean. The remaining blockers are external/prerequisite only: the absent production trust infrastructure and protected live-gate environment that gate the ordered acceptance contract and external-effect phases.

## Checklist
- [x] Record narrow bootstrap-trust path-traversal regression review-fix correction authorizing only `packages/installer/test/operations/bootstrap-trust.test.ts` with supplemental installer full test gate
- [x] Record narrow boundary-metadata correction authorizing only `scripts/boundaries-check.mjs` and `tests/boundaries-check.test.mjs`, with focused regression verification

- [x] Record C21 final candidate `c47e0f70` and tracker completion `670ea72f`
- [x] Record accepted C22–C25 planning correction `4d99e14e`
- [x] Freeze exact corrected C22 ownership and external-effect namespaces
- [x] Freeze the sole ordered eleven-command acceptance contract and Node 22 environment
- [x] Record external/product prerequisites without scheduler-blocking locally reachable C22 implementation
- [x] Mark C22 in-progress under an active tracker-only claim from `4d99e14e`
- [x] Mark C23–C25 dependency-blocked through C22, not deferred by scope
- [x] Implement C22 within exact owned paths (locally reachable implementation complete at candidate `b8e26497`; commits `3a17c899`, `71993228`, `b8e26497`; planning corrections `4d99e14e`, `02672c80`, `1568b617`, `fcf01ffb`)
- [x] Record explicit user decisions: version `1.0.0`, MIT license, public npm access; production trust infrastructure not available yet
- [x] Mark implemented local tooling, metadata, tests, and reviews accurately complete
- [x] Record supplemental green evidence: `release:test` 12/12, boundary tests 4/4, boundaries 15, coherence, command verifier, static-secret scanner, frozen-lock install, pack smoke, installer full test 34/34
- [x] Record ordered acceptance command 1 red with `ROOT_CEREMONY_RECORD_MISSING`; commands 2–11 unrun/blocked by ordered gate and absent witnessed ceremony/evidence, root delegation/KMS/OIDC, retention-locked storage; no acceptance claimed
- [x] Record that local invoking-user Claude/Codex sessions exist but protected live-gate environment is not provisioned
- [x] Complete final blocker/high locally reachable reviews (clean; remaining blockers are external/prerequisite only)
- [ ] Integrate and review the offline root ceremony and delegation evidence before signing or upload (blocked: production trust infrastructure not provisioned)
- [ ] Run all eleven acceptance commands in exact order (blocked: command 1 red, commands 2–11 gated)
- [ ] Record final candidate, external-effect receipt heads, completion, and C23 eligibility (blocked: acceptance and external-effect phases not complete)
