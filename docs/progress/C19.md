# C19 Execution Ledger

- Status: `in-progress`; active tracker-only claim from planning-correction base `cf58230c`. No implementation or acceptance evidence is claimed.
- Dependency: C18 is complete with final candidate `570c2dc5` and completion commit `7d86a089`; planning correction `cf58230c` is integrated before this claim.
- Ownership: exactly `packages/installer/src/{journal,trust,migrations,consent}/**`, `packages/installer/test/{journal,trust,migrations,consent}/**`, `packages/installer/src/index.ts` solely for additive public C19 exports, `packages/installer/package.json` solely for package scripts and dependencies required by `typecheck`, `test:journal-crash`, and `test:trust-consent`, `pnpm-lock.yaml` only for those installer manifest changes under the package-changing manifest/lock rule, root `package.json` solely for `scripts["compat-train:build"]` and `scripts["compat-train:verify"]`, `tests/fixtures/{install-homes,journals,compat-train}/**`, `scripts/compat-train/**`, `docs/install.md`, `docs/migrations.md`, `docs/compatibility.md`, `.changeset/C19-installer.md`, and ledger/integration artifacts. Repair/uninstall-after-upgrade coverage is limited to journal/migration state-machine transitions and fixture-level recovery; host mutation operations remain C20-owned.
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`.
- Five mandatory ordered acceptance gates:
  1. `corepack pnpm --filter @horseness/installer run typecheck`
  2. `corepack pnpm --filter @horseness/installer run test:journal-crash`
  3. `corepack pnpm --filter @horseness/installer run test:trust-consent`
  4. `corepack pnpm run compat-train:build -- --version 0.0.0-compat.1`
  5. `corepack pnpm run compat-train:verify -- --version 0.0.0-compat.1`
- Scheduling: C19 is the sole active claim. C20 is blocked by C19; C21–C25 remain dependency-blocked transitively through C20.

## Checklist

- [x] Record C18 final candidate `570c2dc5` and completion `7d86a089`
- [x] Record integrated C19 planning correction and claim base `cf58230c`
- [x] Freeze exact corrected C19 ownership and C20 boundary
- [x] Freeze the five exact ordered Node 22 acceptance commands
- [x] Mark C19 in-progress with an active tracker-only claim
- [x] Mark C20 blocked by C19
- [ ] Implement C19 within exact ownership
- [ ] Run all five acceptance gates in order under the recorded environment
- [ ] Complete blocker/high review and integrate any review fixes
- [ ] Record final candidate and complete C19
