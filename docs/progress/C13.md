# C13 Execution Ledger

- Status: `complete`
- Dependency: C12 is complete with final candidate `793d2608`, tracker completion commit `26b700f8`; its exact ordered Node 22 gates passed and final code and security reviews were clean.
- Claim base: `26b700f8`; the C13 tracker-only claim MUST be integrated before any C13 implementation edit.
- Source-owned paths: exactly `apps/daemon/**`, `packages/protocol/src/transports/**`, `packages/protocol/test/transports/**`, `.github/workflows/ci.yml`, affected package manifests (`apps/daemon/package.json`, `packages/protocol/package.json`), `pnpm-lock.yaml`, `.changeset/C13-daemon-transports.md`, and `docs/compatibility.md` where affected. The conventional tracker claim/completion transactions separately own only `docs/progress.md` and `docs/progress/C13.md`.
- Package-changing scope: C13 may change manifests for `@horseness/daemon` and `@horseness/protocol`, plus the root `package.json` only for exact forwarding scripts required by the frozen acceptance commands. `pnpm-lock.yaml` is owned when manifest serialization changes and MUST be generated by pnpm. No other package manifest, application, adapter, installer, CLI, policy, domain, orchestrator, or store source is owned by C13.
- Deliverables: implement the local daemon that serves bootstrapped local authority over permission-restricted transports (stdio and Unix socket/Windows named pipe; TCP disabled by default). Implement protocol transport layer in `packages/protocol/src/transports/**` with inspection, authentication, and JSON-RPC framing. Implement first-authority bootstrap ceremony: atomic compare-and-swap consumes an owner-only bootstrap capability bound to workspace ID and local OS identity exactly once to append the first authority/grant events. Expose orchestrator services over authenticated transports.
- Transport security: stdio is process-inherited and peer-verified. Unix socket uses owner-only `0600` in a `0700` directory with realpath confinement and symlink rejection. Windows named pipe uses a non-inheriting owner-only DACL. TCP is disabled by default; enabling it requires explicit configuration and still requires authenticated grants. All transports use `TransportInspectionV1` and `inspectAndAuthenticateTransportV1` from the protocol package.
- Bootstrap ceremony: installation creates an owner-only bootstrap capability (POSIX `0600` in `0700` dir, Windows non-inheriting owner-only DACL) bound to workspace ID and invoking local OS account. An atomic compare-and-swap consumes it exactly once to append the first authority/grant events. Concurrent or different-user attempts fail closed. The daemon serves this ceremony over local transport.
- Multi-process and recovery: daemon survives crash and restarts; restored workspace rebinding requires explicit credential rebinding after replay. Multi-user isolation is enforced by OS identity binding. Endpoint discovery uses owner-only state files and authenticated startup.
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; ordered gates 1, 2, and 4 passed with exit 0. By explicit user direction on 2026-08-13, exact gate 3 is waived/unobserved solely for C13 repository chunk completion; it did not pass and no cross-OS validation is claimed.
- Acceptance commands (ordered):
  1. `corepack pnpm --filter @horseness/daemon --filter @horseness/protocol run typecheck`
  2. `corepack pnpm --filter @horseness/daemon run test:multiprocess`
  3. `corepack pnpm run ci:require-os-receipts -- C13 linux macos windows` (OS)
  4. `corepack pnpm run boundaries:check`
- Completion checkpoint: `feat(daemon): serve bootstrapped local authority`.
- Final candidate: `7cc39529`.
- Completion tracker base: `ad97c417`; this documentation correction prepares completion state only, and Main will create the serialized completion commit.
- Gate evidence: exact gate 1 passed (daemon + protocol typecheck); exact gate 2 passed (multiprocess 6/6); exact gate 3 is explicitly waived/unobserved by user direction dated 2026-08-13, with no candidate-bound Linux/macOS/Windows receipts and no cross-OS validation claim; exact gate 4 passed (boundaries 14/14). Supplemental protocol transports 9/9, store typecheck, and authority-state 3/3 passed under the recorded Node 22 environment.
- Review evidence: iterative blocker/high code review resolved atomic bootstrap/grant authority, per-connection opaque grant authentication under the frozen owner-DAC boundary, authenticated restore rebinding, strict CAS/digest grant state, endpoint startup compensation, complete grant lifecycle RPCs, active-parent delegation, expiry cap, and full resource-scope narrowing. Definitive code review and final security review are clean.
- Scheduling: C13 is complete at final candidate `7cc39529`; C14 is the sole next dependency-ready chunk.
- Planning correction: the 2026-08-13 waiver applies only to exact gate 3 for C13 ordinary repository chunk completion. The CI matrix and verifier remain intact for future evidence; runtime transport/bootstrap authentication is unchanged; no C20, release, publication, promotion, or other gate precedent is created. The missing cross-OS receipts remain honestly unobserved and are not an active C13 blocker under this explicit direction.

## Checklist

- [x] C12 dependency evidence recorded: final candidate `793d2608`, completion `26b700f8`
- [x] Serialized tracker-only C13 claim prepared from integrated C12 completion base `26b700f8`
- [x] Exact C13 source ownership and package-changing scope recorded
- [x] Daemon, transport, bootstrap, multi-process, and security contracts recorded
- [x] Exact Node 22 environment and four ordered acceptance commands recorded
- [x] Implement `@horseness/daemon` local authority serving over authenticated transports
- [x] Implement `packages/protocol/src/transports/**` transport layer with inspection and framing
- [x] Implement first-authority bootstrap ceremony with atomic CAS and OS identity binding
- [x] Implement concurrent/unauthorized bootstrap rejection, revoked/stale token handling, restored workspace rebinding, multi-user isolation, and endpoint discovery
- [x] Run exact gates 1, 2, and 4 in order under the recorded Node 22 environment; record exact gate 3 as explicitly waived/unobserved, not passed
- [x] Complete blocker/high code and security review and resolve every accepted finding
- [x] Record the final candidate and exact gate evidence in both tracker files
- [x] Prepare tracker completion state making C14 the sole next dependency-ready chunk; Main will create the completion commit
# Reviewed ownership correction

- `packages/store-sqlite/src/sqlite-authority.ts`, `packages/store-sqlite/src/index.ts`, and focused `packages/store-sqlite/test/**` are authorized for C13 because first-authority bootstrap must atomically commit workspace genesis with the initial authenticated authority/grant state. Keeping this transaction in the daemon would create an unrecoverable crash window between genesis and usable authority. The correction is deliberately generic (`authority state`), not daemon-specific, and also supplies authenticated compare-and-swap reads needed for restore/rebind.

