# C21 Execution Ledger

- Status: `in-progress` under the tracker-only claim from dependency-ready base `6eea20a1`; the two public regressions below are reproduced, but their source fixes and C21 acceptance remain incomplete.
- Dependency: C20 is complete with final source candidate `e49c5e1c` and tracker completion `6eea20a1`.
- Waiver boundary: accepted ADR 0008 waived only former C20 OS-receipt gate 8. It is nonprecedential and does not alter C21's security, system, install-recovery, four-host closed-loop, or candidate-bound Claude/Codex host-distinct receipt obligations.
- Ownership: primary C21 ownership remains exactly `tests/security/**`, `tests/system/**`, `SECURITY.md`, root `package.json`, `pnpm-lock.yaml`, `docs/progress/C21.md`, and `docs/progress.md`. Narrow review-fix ownership additionally permits only `apps/daemon/src/daemon.ts`, `apps/daemon/src/discovery.ts`, `apps/daemon/test/server.test.ts`, `apps/daemon/test/multiprocess.test.ts`, `packages/installer/src/journal/index.ts`, and `packages/installer/test/journal/journal.test.ts`, solely for the two defects recorded below.
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`.
- Ordered acceptance commands:
  1. `corepack pnpm run test:security`
  2. `corepack pnpm run test:system`
  3. `corepack pnpm run test:closed-loop -- --hosts pi,omp,claude,codex`
- Scheduling: C21 is the sole active claim. C22 remains not-started and dependency-blocked by C21; C23–C25 remain transitively dependency-blocked.

## Public-regression review correction

- Daemon reproduction: `corepack pnpm exec node --test --test-name-pattern="daemon discovery rejects a stale endpoint whose live PID belongs to another process incarnation" tests/system/recovery-regressions.test.mjs` reproduces that discovery accepts a stale authority record when its recorded PID is live but belongs to a different process incarnation, reaching `STALE_ENDPOINT_PID_REUSE_NOT_REJECTED`. The authorized fix is limited to persisting and verifying process incarnation in daemon ownership/discovery state; focused daemon tests must also prove that a genuine current owner is never reclaimed.
- Installer reproduction: `corepack pnpm exec node --test --test-name-pattern="installer journal reclaims an abandoned lock and completes the durable append" tests/system/recovery-regressions.test.mjs` reproduces that an abandoned `.generation-1.lock` cannot be safely reclaimed, so the append probe is killed at its bounded timeout instead of completing. The authorized fix is limited to a durable journal lock owner bound to nonce, PID, and verifiable process incarnation, with bounded fail-closed stale-lock recovery; focused installer tests must also prove that a genuine current owner is never reclaimed.
- Supplemental review-fix gates, to run after source correction under the recorded Node 22 environment:
  1. `corepack pnpm --filter @horseness/daemon run typecheck`
  2. `corepack pnpm --filter @horseness/daemon run test`
  3. `corepack pnpm --filter @horseness/installer run typecheck`
  4. `corepack pnpm --filter @horseness/installer run test:journal-crash`
- These supplemental gates do not replace or reorder the three primary C21 acceptance commands. No source fix, supplemental-gate success, or primary-gate success is recorded by this correction; C21 remains `in-progress`.

## Checklist

- [x] Record C20 final source candidate `e49c5e1c` and completion `6eea20a1`
- [x] Record accepted ADR 0008 as a C20-only, nonprecedential waiver that does not alter C21
- [x] Freeze exact C21 ownership and the three ordered Node 22 acceptance commands
- [x] Mark C21 in-progress under a tracker-only claim from base `6eea20a1`
- [x] Mark C22 blocked by C21
- [x] Record the narrow public-regression review-fix paths, both concrete reproductions, and the four supplemental package gates without claiming a source fix or green acceptance
- [ ] Implement C21 security, resilience, recovery, and four-host closed-loop validation within owned paths
- [ ] Run all three ordered acceptance commands under the recorded Node 22 environment
- [ ] Run the four supplemental daemon/installer review-fix gates under the recorded Node 22 environment
- [ ] Complete blocker/high code, security, and task-accounting review
- [ ] Record the final source candidate and observed evidence in a separate tracker-only completion transaction
- [ ] Clear the active claim and make C22 sole next eligible
