# C21 Execution Ledger

- Status: `in-progress` at current source candidate `0d0e2784` under the tracker-only claim from dependency-ready base `6eea20a1`; implementation and every observed local gate except the exact four-host closed-loop are complete, and completion remains blocked externally on the invoking OS user's expired native Claude OAuth session.
- Dependency: C20 is complete with final source candidate `e49c5e1c` and tracker completion `6eea20a1`.
- Waiver boundary: accepted ADR 0008 waived only former C20 OS-receipt gate 8. It is nonprecedential and does not alter C21's security, system, install-recovery, four-host closed-loop, or candidate-bound Claude/Codex host-distinct receipt obligations.
- Ownership: primary C21 ownership remains exactly `tests/security/**`, `tests/system/**`, `SECURITY.md`, root `package.json`, `pnpm-lock.yaml`, `docs/progress/C21.md`, and `docs/progress.md`. Narrow review-fix ownership additionally permits only `apps/daemon/src/daemon.ts`, `apps/daemon/src/discovery.ts`, `apps/daemon/test/server.test.ts`, `apps/daemon/test/multiprocess.test.ts`, `packages/installer/src/journal/index.ts`, and `packages/installer/test/journal/journal.test.ts`, solely for the two defects recorded below.
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`.
- Ordered acceptance commands:
  1. `corepack pnpm run test:security`
  2. `corepack pnpm run test:system`
  3. `corepack pnpm run test:closed-loop -- --hosts pi,omp,claude,codex`
- Scheduling: C21 is the sole active claim. C22 remains not-started and dependency-blocked by C21; C23–C25 remain transitively dependency-blocked.

## Implementation and observed evidence

- Planning correction: `e032d946` narrowly authorized the daemon process-incarnation and installer journal-lock lifecycle corrections recorded below.
- Implementation: commits `28c4f961`, `3802437e`, and current source candidate `0d0e2784` implement C21 validation and the reviewed lifecycle fixes.
- Daemon correction: ownership/discovery state now persists and verifies process incarnation, rejects a stale authority record whose live PID belongs to a different incarnation, and preserves a genuine current owner. Supplemental daemon typecheck passed and the full daemon tests passed 9/9.
- Installer correction: the journal lock owner is durably bound to nonce, PID, and verifiable process incarnation with bounded fail-closed stale-lock recovery, while a genuine current owner is not reclaimed. Supplemental installer typecheck passed and journal-crash tests passed 13/13.
- Primary security: `corepack pnpm run test:security` passed 6/6 under the recorded Node 22 environment.
- Primary system: `corepack pnpm run test:system` initially failed with 2 findings; after the lifecycle fix it passed 10/10 under the recorded Node 22 environment.
- Reviews: definitive blocker/high reviews are clean after the lifecycle fix.
- Closed-loop: the exact gate `corepack pnpm run test:closed-loop -- --hosts pi,omp,claude,codex` first failed argument parsing. After commit `0d0e2784` fixed the argument path, the same exact gate reached the real pinned Claude Code `2.1.228` host and remains red because native auth reports `loggedIn: false` and `authMethod: none`; safe-mode evidence proves expired first-party OAuth. Pi, OMP, and Codex are not claimed sufficient independently unless this exact script observes them.
- External blocker: the invoking OS user must run normal interactive `claude auth login`, then the exact closed-loop gate must be rerun. No repository change, API key, isolated config, or skip is permitted. Until then C21 completion remains unchecked and C22–C25 remain transitively dependency-blocked at C21.

## Checklist

- [x] Record C20 final source candidate `e49c5e1c` and completion `6eea20a1`
- [x] Record accepted ADR 0008 as a C20-only, nonprecedential waiver that does not alter C21
- [x] Freeze exact C21 ownership and the three ordered Node 22 acceptance commands
- [x] Mark C21 in-progress under a tracker-only claim from base `6eea20a1`
- [x] Mark C22 blocked by C21
- [x] Record planning correction `e032d946`, implementation commits `28c4f961`, `3802437e`, and current source candidate `0d0e2784`
- [x] Implement C21 security, resilience, recovery, and four-host closed-loop validation within owned paths
- [x] Run supplemental daemon typecheck and full tests green 9/9
- [x] Run supplemental installer typecheck and journal-crash green 13/13
- [x] Run primary security green 6/6
- [x] Fix the 2 primary system findings and rerun primary system green 10/10
- [x] Complete definitive blocker/high reviews clean after the lifecycle fix
- [ ] Rerun the exact four-host closed-loop gate after the invoking OS user completes normal interactive `claude auth login`; do not substitute an API key, isolated config, repository change, or skip
- [ ] Record C21 completion in a separate tracker-only completion transaction
- [ ] Clear the active claim and make C22 sole next eligible
