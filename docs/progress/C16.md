# C16 Execution Ledger

- Status: `in-progress`
- Dependency: C15 is complete at final candidate `33b34f3d` with completion commit `7773edbf`; all five exact C15 primary gates and the recorded supplemental verification passed under the recorded Node 22 environment, and final blocker/high code and security review was clean.
- Claim base: tracker-only C16 claim prepared from current integrated HEAD `7773edbf` before implementation edits.
- Source-owned paths: exactly `adapters/omp/**`, `adapters/omp/package.json`, `tests/fixtures/hosts/omp/**`, `pnpm-lock.yaml`, `docs/integrations/omp.md`, `docs/compatibility.md`, `.changeset/C16-omp.md`, ledger/integration artifacts, and root `package.json` only for the `scripts["host:smoke:omp"]` forwarding key. The conventional tracker claim/completion transactions separately own only `docs/progress.md` and `docs/progress/C16.md`.
- Package-changing scope: C16 may change the `@horseness/adapter-omp` manifest and `pnpm-lock.yaml` as applicable; lockfile changes MUST be generated by pnpm. Root `package.json` ownership is limited exactly to the `scripts["host:smoke:omp"]` forwarding key, whose implementation remains adapter-owned. C16 does not own `scripts/host-feasibility/**`, any other root manifest key, or Pi, Claude, Codex, daemon, CLI, SDK, installer, policy, domain, orchestrator, store, CI workflow, or unrelated package paths.
- Deliverables: ship the validated OMP native Horseness bundle and adapter-owned smoke implementation. The native smoke matrix must satisfy the C16 contract without harness synthesis.
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; every ordered command expects exit 0. No command may be skipped, omitted, duplicated, reordered, or substituted.
- Acceptance commands (ordered):
  1. `corepack pnpm --filter @horseness/adapter-omp run typecheck`
  2. `corepack pnpm --filter @horseness/adapter-omp run test`
  3. `corepack pnpm --filter @horseness/adapter-omp pack`
  4. `corepack pnpm run host:smoke:omp`
  5. `corepack pnpm run boundaries:check`
- Completion checkpoint: `feat(omp): ship validated native Horseness bundle`.
- Scheduling: C16 is the sole active claim. C17 is blocked on C16 and is not started.
- Blockers: no blocker to C16 implementation; C17 remains dependency-blocked.

## Checklist

- [x] C15 dependency evidence recorded: final candidate `33b34f3d`, completion `7773edbf`
- [x] Serialized tracker-only C16 claim prepared from integrated base `7773edbf`
- [x] Exact C16 source ownership, manifest/lock scope, root `scripts["host:smoke:omp"]` key restriction, adapter-owned smoke implementation, and tracker-only transactions recorded
- [x] Exact Node 22 environment and five ordered acceptance commands recorded
- [ ] Integrate tracker-only C16 claim before implementation edits
- [ ] Implement the validated OMP native bundle and adapter-owned `host:smoke:omp` forwarding target within exact ownership
- [ ] Run the five exact acceptance commands in order under the recorded Node 22 environment
- [ ] Complete blocker/high code and security review and resolve every accepted finding
- [ ] Record implementation, review-fix, final-candidate, and exact gate evidence in both tracker files
- [ ] Complete C16 and make C17 the sole next eligible chunk
