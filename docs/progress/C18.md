# C18 Execution Ledger

- Status: `in-progress`; tracker-only claim from current integrated HEAD `f597abe6b9aa305a3a697edaf89178fe62c12a60`. No implementation, acceptance gate, review, or completion is claimed.
- Dependency: C17 is complete with final candidate `145dd0f9` and completion commit `f597abe6`.
- Authentication decision: accepted ADR 0006 governs C18. The pinned, provenance-verified Codex executable may use the invoking OS user's existing logged-in Codex subscription session for provider authentication and billing. Horseness authorization remains a separate opaque, least-privilege, immutable attempt grant. Horseness must not extract, receive, copy, hash, persist, log, or receipt native authentication secrets; a missing or unusable session must fail the smoke closed. No credential-reference prerequisite or controlled test account is required or claimed.
- Ownership: exactly `adapters/codex/**`, `adapters/codex/package.json`, root `package.json` only for the `scripts["host:smoke:codex"]` forwarding key, `tests/fixtures/hosts/codex/**`, `pnpm-lock.yaml`, `docs/integrations/codex.md`, `docs/compatibility.md`, `.changeset/C18-codex.md`, and ledger/integration artifacts. The root command must forward to an adapter-owned smoke implementation. C18 does not own `scripts/host-feasibility/**` or shared C11 feasibility schemas/scripts.
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`.
- Five mandatory ordered acceptance gates:
  1. `corepack pnpm --filter @horseness/adapter-codex run typecheck`
  2. `corepack pnpm --filter @horseness/adapter-codex run test`
  3. `corepack pnpm --filter @horseness/adapter-codex pack`
  4. `corepack pnpm run host:smoke:codex`
  5. `corepack pnpm run boundaries:check`
- Native contribution requirement: `host:smoke:codex` must use the pinned real Codex distribution, an actual mandatory plugin/skill/MCP contribution, and the invoking user's existing logged-in Codex subscription session. CLI-only operation is additional and never substitutes for the native contribution or complete `WorkerReturnV1` matrix.
- Scheduling: C18 is the sole active claim. C19 is blocked by C18; C20–C25 remain dependency-blocked transitively through C18.

## Checklist

- [x] Record C17 final candidate `145dd0f9` and completion `f597abe6`
- [x] Record accepted ADR 0006 subscription-session authentication for C18
- [x] Freeze exact C18 ownership and exclusions
- [x] Freeze the five exact ordered Node 22 acceptance commands
- [x] Record mandatory real Codex plugin/skill/MCP contribution and non-substitutive CLI-only operation
- [x] Record tracker-only active claim from HEAD `f597abe6b9aa305a3a697edaf89178fe62c12a60`
- [x] Mark C19 blocked by C18
- [ ] Implement C18 within exact ownership
- [ ] Run all five acceptance gates in order under the recorded environment
- [ ] Complete definitive blocker/high review and resolve all findings
- [ ] Record final candidate and completion
