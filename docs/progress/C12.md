# C12 Execution Ledger

- Status: `in-progress`
- Dependency: C11 is complete with final candidate `05c5f4c6`, tracker completion commit `8987a1af`, and lessons commit `7defa3f1`; its exact ordered Node 22 gates passed and final code and security reviews were clean.
- Claim base: `71b990bbc1edbaf5152a5b201ad0ed311b8422e5`; replace this placeholder with the full integrated tracker-repair commit SHA when that commit exists. The C12 tracker-only claim MUST be integrated before any C12 implementation edit.
- Source-owned paths: exactly `packages/sdk/**`, `packages/adapter-kit/**`, `docs/compatibility.md`, the C12-owned package manifests, `pnpm-lock.yaml`, and `.changeset/C12-sdk-adapter-kit.md`. The conventional tracker claim/completion transactions separately own only `docs/progress.md` and `docs/progress/C12.md`.
- Package-changing scope: C12 may change only the manifests for `@horseness/sdk` and `@horseness/adapter-kit`, plus the root `package.json` only for the exact `adapter:conformance` forwarding script required by the frozen acceptance command. `pnpm-lock.yaml` is owned when manifest serialization changes and MUST be generated by pnpm. No other package manifest, application, adapter, installer, daemon, CLI, protocol, policy, domain, orchestrator, or store source is owned by C12.
- Deliverables: implement `@horseness/sdk` as the sole typed core-facing client surface used by adapters and `@horseness/adapter-kit` as the secure adapter SPI, declarative install contribution format, process-boundary utilities, fixtures, and conformance harness. Expose the complete typed coordinator and worker surfaces frozen by the protocol without granting adapters authority to decide admission, schedule work, mutate canonical state directly, or access internal stores.
- Worker return contract: C12 owns the typed `WorkerReturnV1` flow for artifact/evidence publication, receipt submission, proposal sealing/submission, decision subscription and resume, and surfacing accepted, rejected, conflicted, quarantined, and approval-required decisions. Harness-synthesized worker returns are forbidden; later native adapters must originate this entire return path through the SPI.
- Lifecycle contract: the SPI MUST cover capability detection, context injection, launch, cancel, reconcile, reconnect/reattach, native resume, receipt collection, native-package metadata, declarative install contributions, and read-only doctor probes. Reconcile, reattach, adoption, and native resume of an existing provider operation MUST retain the original immutable attempt generation, ForkPin, context manifest/binding, provider identity, and idempotency key; changed context requires an authorized new generation and binding.
- Security invariants: adapters MUST NOT import store/orchestrator internals, write SQLite, decide admission, implement scheduling, synthesize admission outcomes, independently modify host configuration, or receive secret credential values. Credentials remain opaque keychain/host references under least-privilege workspace/attempt grants. Process launch MUST use argv arrays, realpath-confined executable and cwd paths, an allowlisted environment with credentials removed by default, bounded/redacted outputs and receipts, and fail-closed workspace/task/generation/binding checks.
- Install and doctor invariants: install contributions are declarative data only; adapters do not write installation targets. The versioned doctor result is read-only, bounded, and redacted, reports checks as `ok|warning|error` with stable codes, evidence digests, and a restart-required flag, and checks credential-reference availability without reading secret values. Repair remains a separate mutating operation outside C12.
- Conformance requirements: executable conformance MUST cover launch/cancel/reconcile/reattach/resume, immutable binding retention, credential-reference handling and redaction, least-privilege and cross-scope rejection, process path/realpath and environment confinement, bounded outputs/evidence, duplicate callback idempotency, `WorkerReturnV1` decision subscription/resume and surfacing, versioned doctor schema, declarative install contributions, denied direct authority escalation, and package-boundary enforcement.
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; every ordered command expects exit 0. No command may be skipped, omitted, duplicated, reordered, or substituted.
- Acceptance commands (ordered):
  1. `corepack pnpm --filter @horseness/sdk --filter @horseness/adapter-kit run typecheck`
  2. `corepack pnpm --filter @horseness/sdk --filter @horseness/adapter-kit run test`
  3. `corepack pnpm run adapter:conformance`
  4. `corepack pnpm run boundaries:check`
- Completion checkpoint: `feat(sdk): add secure host integration contracts`.
- Scheduling: C12 is the sole active claim. C13 remains dependency-ineligible until C12 implementation, the exact ordered acceptance commands, blocker/high review, all required fixes, and tracker completion are integrated.
- Blockers: none at claim time.

## Checklist

- [x] C11 dependency evidence recorded: final candidate `05c5f4c6`, completion `8987a1af`, lessons `7defa3f1`
- [x] Serialized tracker-only C12 claim prepared from placeholder integrated base `71b990bbc1edbaf5152a5b201ad0ed311b8422e5`
- [x] Exact C12 source ownership and package-changing scope recorded
- [x] SDK, adapter SPI, `WorkerReturnV1`, lifecycle, install, doctor, and security contracts recorded
- [x] Exact Node 22 environment and four ordered acceptance commands recorded
- [ ] Replace `71b990bbc1edbaf5152a5b201ad0ed311b8422e5` with the full integrated tracker-repair commit SHA
- [ ] Integrate the tracker-only C12 claim before implementation edits
- [ ] Implement `@horseness/sdk` typed coordinator and worker surfaces within exact ownership
- [ ] Implement `@horseness/adapter-kit` secure SPI, declarative install model, doctor schema, fixtures, and conformance harness within exact ownership
- [ ] Implement and prove the complete `WorkerReturnV1` publication, submission, decision subscription/resume, and decision-surfacing flow
- [ ] Complete security, authority-escalation, credential-reference, confinement, redaction, binding, idempotency, install, and doctor conformance coverage
- [ ] Run the exact acceptance commands in order under the recorded Node 22 environment
- [ ] Complete blocker/high code and security review and resolve every accepted finding
- [ ] Record the final candidate and exact gate evidence in both tracker files
- [ ] Integrate tracker completion and make C13 the sole next dependency-ready chunk
