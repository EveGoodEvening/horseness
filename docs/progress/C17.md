# C17 Execution Ledger

- Status: `not-started`; blocked before claim.
- Dependency: C16 is complete; its tracker records the final implementation, review, and acceptance evidence. Dependency readiness alone does not make C17 claimable while its controlled-live prerequisite is unavailable.
- Claim: none. No tracker-only claim has been integrated and no C17 implementation has started.
- Owned paths when unblocked: exactly `adapters/claude/**`, `adapters/claude/package.json`, `tests/fixtures/hosts/claude/**`, `pnpm-lock.yaml`, `docs/integrations/claude.md`, `docs/compatibility.md`, `.changeset/C17-claude.md`, ledger/integration artifacts, and root `package.json` only for the `scripts["host:smoke:claude"]` forwarding key. The smoke implementation remains adapter-owned; C17 does not own `scripts/host-feasibility/**`.
- Acceptance environment when unblocked: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; every ordered command expects exit 0 and no command may be skipped, omitted, reordered, or substituted.
- Acceptance commands (not run):
  1. `corepack pnpm --filter @horseness/adapter-claude run typecheck`
  2. `corepack pnpm --filter @horseness/adapter-claude run test`
  3. `corepack pnpm --filter @horseness/adapter-claude pack`
  4. `corepack pnpm run host:smoke:claude`
  5. `corepack pnpm run boundaries:check`
- Pre-claim checks: reviewed the C11 Claude scope decision, the frozen C17 ownership and acceptance contract, the Claude feasibility manifest's `HORSENESS_CLAUDE_CREDENTIAL_REF` policy, the controlled-live credential rules, and the current process environment. `HORSENESS_CLAUDE_CREDENTIAL_REF` is absent, and no allowlisted resolver for a controlled-live Claude test account is available in this environment.
- Why C11 does not satisfy C17: C11's optional credential-free hermetic validation established only real native artifact loading and official plugin validation. It explicitly reported `contextInjection`, `deterministicProviderAttempt`, `receiptBinding`, `restartReconcile`, `resume`, `forkSwitch`, and `uninstall` as unavailable credential-free and deferred their live proof to C17. Reusing C11's optional skip or reduced hermetic matrix would silently weaken C17's required native smoke and is prohibited.
- Blocker: C17 requires a publication-grade, fail-closed credentialed-live native Claude smoke. Before claim, provide an allowlisted resolver for an opaque `HORSENESS_CLAUDE_CREDENTIAL_REF` bound to a controlled-live, least-privilege Claude test account, subject to the frozen network, timeout, cost, provenance, output-bound, and redaction controls. Missing or invalid credentials must fail; they cannot be treated as a successful skip for C17 acceptance.
- Scheduling: no active claim. C17 remains not-started and blocked. C18–C25 remain not-started and dependency-blocked transitively through C17; they are not deferred by scope.

## Checklist

- [x] Confirm C16 dependency completion is recorded
- [x] Review C11 Claude hermetic scope and explicit C17 credentialed-live deferral
- [x] Review C17 exact ownership and ordered acceptance commands
- [x] Confirm `HORSENESS_CLAUDE_CREDENTIAL_REF` is absent from the current environment
- [x] Confirm no allowlisted controlled-live Claude account resolver is available
- [x] Record the exact prerequisite and fail-closed requirement
- [ ] Integrate a tracker-only C17 claim after the prerequisite is available
- [ ] Implement C17 within exact ownership
- [ ] Run the five exact acceptance commands in order
- [ ] Complete blocker/high review and record final candidate
- [ ] Complete C17 and make C18 dependency-ready
