# C17 Execution Ledger

- Status: `complete`; planning correction `8f04cdde`, tracker-only claim `06c83b45`, implementation `cdc4d569`, review fixes `15b15f2b`, `cf0c0cf3`, `356a6f4f`, `7a49f00f`, and final candidate `145dd0f9` are integrated.
- Dependency: C16 is complete at completion commit `9668df34`; dependency-scope correction `7871d94c` removed the unused out-of-scope root OMP dependency and canonically cleaned its lock closure without changing implementation behavior. ADR 0006 and subscription-session planning correction `8f04cdde` completed independent architecture and security review before claim `06c83b45`.
- Ownership: implementation remained within the recorded C17 paths: `adapters/claude/**`, `adapters/claude/package.json`, `tests/fixtures/hosts/claude/**`, `pnpm-lock.yaml`, `docs/integrations/claude.md`, `docs/compatibility.md`, `.changeset/C17-claude.md`, ledger/integration artifacts, and root `package.json` only for the `scripts["host:smoke:claude"]` forwarding key. The smoke implementation is adapter-owned; C17 did not take ownership of `scripts/host-feasibility/**` or shared C11 feasibility schemas/scripts.
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; all five ordered commands completed successfully without omission, reordering, or substitution.
- Acceptance commands and evidence:
  1. `corepack pnpm --filter @horseness/adapter-claude run typecheck` — green.
  2. `corepack pnpm --filter @horseness/adapter-claude run test` — green, 8/8 tests.
  3. `corepack pnpm --filter @horseness/adapter-claude pack` — green.
  4. `corepack pnpm run host:smoke:claude` — green against real Claude Code `2.1.228` using the invoking OS user's existing logged-in subscription session. The smoke proved a candidate-bound `authMode` receipt, a real MCP batch, all five authority-produced outcomes, canonical revision `1` / document value `2`, resume and fork switching, and crash-idempotent native uninstall.
  5. `corepack pnpm run boundaries:check` — green, 14/14.
- Authentication and privacy: the bounded noninteractive native preflight succeeded without extracting, receiving, copying, hashing, persisting, logging, or receipting native authentication secrets. The candidate-bound receipt records the permitted subscription-session `authMode` and required candidate, command/scenario, native/contribution provenance, Horseness identity, output/evidence digest, redaction, timing, and terminal bindings while omitting account identity, email, subscription identifiers, auth paths, token fingerprints, credential references, headers, secret-derived hashes, and secret material.
- Scope evidence: C11's credential-free hermetic validation remains historically accurate and distinct. C17 freshly proved the complete real-native subscription-session matrix, including context injection, deterministic provider attempt, receipt binding, restart reconciliation, resume, fork switching, uninstall, real MCP transport, and all five authority outcomes. Uninstall revokes Horseness grants and contribution discovery only; it makes no Claude logout or provider-session revocation claim.
- Review: definitive blocker/high review of final candidate `145dd0f9` is clean.
- Scheduling: C17 is complete, there are no active claims or blockers, and C18 is the sole next eligible chunk. C19–C25 remain not-started and dependency-blocked transitively through C18.

## Checklist

- [x] Confirm C16 dependency completion is recorded
- [x] Preserve the historical C11 scope and former controlled-account blocker as accurate evidence
- [x] Review C17 exact ownership and unchanged ordered acceptance commands
- [x] Record explicit authorization for existing-user subscription-session provider authentication
- [x] Preserve Horseness least-privilege grants, secret non-extraction, provenance, redaction, deterministic bounds, hard failure, receipt privacy, and uninstall limitation
- [x] Integrate the independently reviewed planning correction `8f04cdde` and ADR 0006
- [x] Perform a fresh bounded fail-closed native subscription-session preflight during implementation/acceptance
- [x] Record tracker-only claim `06c83b45`
- [x] Implement C17 at `cdc4d569` within exact ownership
- [x] Integrate review fixes `15b15f2b`, `cf0c0cf3`, `356a6f4f`, `7a49f00f`, and final candidate `145dd0f9`
- [x] Run the five exact Node 22 acceptance commands in order: typecheck, 8 tests, pack, real Claude smoke, boundaries 14/14
- [x] Prove candidate-bound subscription-session `authMode`, real MCP batch, five authority outcomes, canonical revision `1` / document value `2`, resume/fork, and crash-idempotent native uninstall
- [x] Complete definitive blocker/high review clean
- [x] Complete C17 and make C18 the sole next eligible chunk
