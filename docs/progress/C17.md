# C17 Execution Ledger

- Status: `in-progress`; tracker-only claim prepared from integrated dependency-ready base `8f04cdde`; no implementation has started.
- Dependency: C16 is complete at completion commit `9668df34`; dependency-scope correction `7871d94c` removed the unused out-of-scope root OMP dependency and canonically cleaned its lock closure without changing implementation behavior. ADR 0006 and the subscription-session planning correction integrated at base `8f04cdde` completed independent architecture and security review.
- Claim: conventional tracker-only C17 claim from base `8f04cdde`. This transaction owns only `docs/progress.md` and `docs/progress/C17.md`; implementation, acceptance gates, and commit are intentionally not performed here.
- Owned paths: exactly `adapters/claude/**`, `adapters/claude/package.json`, `tests/fixtures/hosts/claude/**`, `pnpm-lock.yaml`, `docs/integrations/claude.md`, `docs/compatibility.md`, `.changeset/C17-claude.md`, ledger/integration artifacts, and root `package.json` only for the `scripts["host:smoke:claude"]` forwarding key. The smoke implementation remains adapter-owned; C17 does not own `scripts/host-feasibility/**` or shared C11 feasibility schemas/scripts.
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; every ordered command expects exit 0 and no command may be skipped, omitted, reordered, or substituted.
- Acceptance commands (not run):
  1. `corepack pnpm --filter @horseness/adapter-claude run typecheck`
  2. `corepack pnpm --filter @horseness/adapter-claude run test`
  3. `corepack pnpm --filter @horseness/adapter-claude pack`
  4. `corepack pnpm run host:smoke:claude`
  5. `corepack pnpm run boundaries:check`
- Historical pre-claim check: under the former controlled-live contract, review correctly found `HORSENESS_CLAUDE_CREDENTIAL_REF` absent and no allowlisted resolver for a dedicated least-privilege Claude test account. That blocker was valid under the then-effective contract and is preserved as historical pre-claim evidence.
- Accepted correction: explicit user authorization on 2026-08-14 and accepted ADR 0006 supersede only that provider-authentication prerequisite. C17 may use the pinned real Claude Code executable under the invoking OS user's existing logged-in subscription session. No `HORSENESS_CLAUDE_CREDENTIAL_REF`, resolver, controlled provider account, monetary cap, or token-cost cap is required or claimed. Horseness still supplies an opaque least-privilege attempt grant and must never extract, receive, copy, hash, persist, log, or receipt native authentication secrets.
- Why C11 still does not satisfy C17: C11's credential-free hermetic validation established only real native artifact loading and official plugin validation under its frozen historical contract. It did not prove `contextInjection`, `deterministicProviderAttempt`, `receiptBinding`, `restartReconcile`, `resume`, `forkSwitch`, or `uninstall`. C17 must freshly prove the complete real-native matrix and all five authority-produced outcomes; no optional skip, reduced matrix, hermetic receipt, CLI-only substitute, or harness synthesis is permitted.
- Implementation/acceptance prerequisite: run a bounded noninteractive native preflight confirming a usable existing Claude Code subscription session for the invoking OS user without exposing secret material. Missing, expired, rejected, wrong-host, model-unavailable, interactive-login-required, timed-out, redaction-failed, or native-host-failed sessions hard-fail with a stable redacted reason. Tests must not log in or refresh credentials. Exact request/scenario count, operation/turn maxima, allowed tools/network purpose, byte ceilings, provenance, redaction, and finite wall-clock bounds remain mandatory but are not a provider cost guarantee. This is not a credential-reference prerequisite and does not block the tracker-only claim.
- Acceptance evidence: the Claude live receipt is distinct from C11 hermetic and Codex receipts and binds the candidate, exact command/scenario set, pinned native/contribution provenance, observed model, immutable Horseness workspace/run/task/attempt/ForkPin/context/receipt/proposal identities, output/evidence digests and redaction audit, timing, and stable terminal reason. It omits account identity, email, subscription identifiers, auth paths, token fingerprints, credential references, headers, secret-derived hashes, and secret material. Uninstall revokes Horseness grants and contribution discovery only; it makes no Claude logout or provider-session revocation claim.
- Scheduling: C17 is the sole active claim and is `in-progress`. This claim records authorization and scope, not observed implementation or gate evidence. C18–C25 remain not-started and dependency-blocked transitively through C17.

## Checklist

- [x] Confirm C16 dependency completion is recorded
- [x] Preserve the historical C11 scope and former controlled-account blocker as accurate evidence
- [x] Review C17 exact ownership and unchanged ordered acceptance commands
- [x] Record explicit authorization for existing-user subscription-session provider authentication
- [x] Preserve Horseness least-privilege grants, secret non-extraction, provenance, redaction, deterministic bounds, hard failure, receipt privacy, and uninstall limitation
- [x] Integrate the independently reviewed planning correction and ADR 0006
- [ ] Perform a fresh bounded fail-closed native subscription-session preflight during implementation/acceptance
- [x] Prepare the tracker-only C17 claim from integrated base `8f04cdde`
- [ ] Implement C17 within exact ownership
- [ ] Run the five exact acceptance commands in order
- [ ] Complete blocker/high review and record final candidate
- [ ] Complete C17 and make C18 dependency-ready
