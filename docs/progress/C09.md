# C09 Execution Ledger

- Status: `in-progress`
- Dependencies: C08 complete in `docs/progress/C08.md`; tracker claim commit `d6bfd42`, implementation commit `171b86d`, review-fix commits `f7721d5`, `241a977`, `219f740`, `3c87b2a`, and `2a3c873`, and final candidate `2a3c873` are integrated. All three ordered C08 gates ran under Node 22 with `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`: orchestrator typecheck exit 0, `test:tasks-forks-receipts` 16/16, and `test:model` 6/6. Supplemental trusted-authority verification reported store typecheck exit 0 and 61/61 store tests. The final blocker/high review was clean.
- Claim base: `19bb423`; this tracker-only claim must be integrated before any C09 source edit
- Owned paths: `packages/orchestrator/src/context/**`, `packages/orchestrator/test/context/**`, `docs/context.md`, `packages/orchestrator/package.json`, `pnpm-lock.yaml`, `.changeset/C09-context-reconstruction.md`, `docs/progress/C09.md`, `docs/progress.md`
- Ownership expansion: the exact C09 plan row owns context source/tests and `docs/context.md`; under the plan's package-changing-row rule, C09 additionally owns only the affected orchestrator package manifest, `pnpm-lock.yaml`, its named changeset, and the two serialized tracker files. No other source, test, documentation, manifest, root script, vector, or integration path is authorized.
- Acceptance commands (ordered):
  1. `corepack pnpm --filter @horseness/orchestrator run typecheck`
  2. `corepack pnpm --filter @horseness/orchestrator run test:context-golden`
  3. `corepack pnpm run vectors:verify -- context-binding`
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; expected exit is 0 for all three commands, with no skip, omission, reordering, or substitution permitted
- Blocker: `none`
- Scheduling: C09 is claimed and in progress; C10 remains dependency-ineligible until C09 is completed with its ordered acceptance evidence and clean review disposition
- Expected implementation checkpoint: `feat(orchestrator): reconstruct pinned context manifests`

## Checklist

- [x] C08 dependency completion and exact evidence verified
- [x] Conventional tracker claim prepared from integrated base `19bb423` before source edits
- [x] Exact bounded C09 owned paths and package-changing-row expansion recorded
- [x] Exact three-command acceptance contract and Node 22 environment recorded in order
- [ ] Tracker claim commit integrated before source edits
- [ ] Deterministic context reconstruction implemented within exact ownership
- [ ] Activation, replacement, and deactivation invalidate the correct context epochs
- [ ] Artifact publication crash boundaries fail closed
- [ ] Context manifest bytes are deterministic
- [ ] ForkPin visibility and manifest/binding digest equivalence are verified
- [ ] Exact acceptance commands completed in order under the recorded Node 22 environment
- [ ] Blocker/high review completed and all findings resolved
- [ ] Tracker completion records final candidate/evidence and makes C10 eligible
