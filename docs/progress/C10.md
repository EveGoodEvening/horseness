# C10 Execution Ledger

- Status: `in-progress`
- Dependencies: C09 is complete in `docs/progress/C09.md`; tracker claim commit `33f6529`, implementation commit `f350ba8`, review-fix commits `ff18b7e`, `023c948`, and `0913eb0`, and final candidate `0913eb0` are integrated. All three ordered C09 gates ran under Node 22 with `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`: orchestrator typecheck exit 0, `test:context-golden` 15/15, and `context-binding` vectors 3/3. Supplemental store verification reported typecheck exit 0 and 74/74 tests; the final blocker/high review was clean.
- Claim base: `ddad712`; this serialized tracker claim commit must be integrated before any C10 source edit
- Owned paths: `packages/orchestrator/src/scheduler/**`, `packages/orchestrator/src/attempts/**`, `packages/orchestrator/src/leases/**`, `packages/orchestrator/src/dispatch/**`, `packages/orchestrator/src/recovery/**`, `packages/orchestrator/test/scheduler/**`, `packages/orchestrator/test/attempts/**`, `packages/orchestrator/test/leases/**`, `packages/orchestrator/test/dispatch/**`, `packages/orchestrator/test/recovery/**`, `packages/orchestrator/src/context/reconstruction.ts`, `packages/orchestrator/test/context/**`, `packages/orchestrator/package.json`, `pnpm-lock.yaml`, `.changeset/C10-scheduler-dispatch.md`, `docs/progress/C10.md`, `docs/progress.md`
- Ownership expansion: the exact C10 plan row owns the named scheduler, attempts, leases, dispatch, and recovery source/test subtrees. A narrow reviewed ownership correction resolves the C10 core blocker by additionally authorizing edits to `packages/orchestrator/src/context/reconstruction.ts` and `packages/orchestrator/test/context/**` solely to expose and test an opaque, stable authenticity assertion or capability-consumption API for C09 `ReconstructedContextV1`. This correction MUST NOT expose trust internals or an exported constructor and does not authorize other C09 context changes. Under the package-changing-row rule C10 additionally owns only `packages/orchestrator/package.json`, `pnpm-lock.yaml`, and the explicitly named C10 changeset and tracker ledgers. No orchestrator package-root export, domain, protocol, policy, store, adapter, application, root-manifest, or other shared-file change is authorized; any further cross-boundary need requires another serialized reviewed ownership correction before editing.
- Acceptance commands (ordered):
  1. `corepack pnpm --filter @horseness/orchestrator run typecheck`
  2. `corepack pnpm --filter @horseness/orchestrator run test:dispatch-property`
  3. `corepack pnpm --filter @horseness/orchestrator run test:dispatch-restart-races`
- Acceptance environment: Node 22 with exact environment `CI=1 TZ=UTC LANG=C NODE_OPTIONS=--unhandled-rejections=strict`; expected exit is 0 for all three commands, with no skip, omission, reordering, or substitution permitted
- Blocker: C10 core scheduling/dispatch work cannot safely consume C09 `ReconstructedContextV1` authenticity without a stable opaque assertion or capability-consumption boundary; the reviewed correction above authorizes only that boundary and its focused tests. The blocker remains open until the API is implemented and reviewed without exporting trust internals or a constructor.
- Scheduling: C10 is the sole active claimed chunk; C11 depends on C10 and remains dependency-ineligible until C10 has complete ordered acceptance evidence and a clean blocker/high review
- Expected implementation checkpoint: `feat(orchestrator): schedule replay-safe bound attempts`

## Checklist

- [x] C09 dependency completion and exact evidence verified
- [x] Conventional serialized tracker claim prepared from integrated base `ddad712` before source edits
- [x] Exact bounded C10 ownership and package-changing-row expansion recorded
- [x] Narrow reviewed ownership correction recorded for the C10 core `ReconstructedContextV1` authenticity-consumption blocker
- [x] Exact three-command acceptance contract and Node 22 environment recorded in order
- [ ] Tracker claim commit integrated before source edits
- [ ] Replay-safe scheduler, attempts, leases, dispatch, and recovery implemented within exact ownership
- [ ] Opaque, stable `ReconstructedContextV1` authenticity assertion or capability consumption implemented and tested without exporting trust internals or a constructor
- [ ] No ForkPin, attempt binding, launch intent, or dispatch occurs before durable `DeltaAcceptedV1`
- [ ] Transition, property, restart, crash-window, lookup, late-handle, cancellation, duplicate-authorization, receipt-mismatch, retry-refresh, adoption, reattach, and deterministic-ready-set coverage completed
- [ ] Exact acceptance commands completed in order under the recorded Node 22 environment
- [ ] Blocker/high review completed and all findings resolved
- [ ] Tracker completion records final candidate and evidence and makes C11 dependency-ready
