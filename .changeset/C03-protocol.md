---
"@horseness/protocol": minor
---

Add the versioned JSON-RPC 2.0 coordinator protocol, exhaustive authorization registry, domain wire mappings, adapter SPI, generated schemas, and executable conformance vectors.
