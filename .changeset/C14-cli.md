---
"@horseness/cli": minor
---

Add the packed `horseness` executable, registry-driven coordinator commands, authenticated local daemon lifecycle and credential recovery operations, deterministic JSON and redacted human output, and a fresh-daemon packed smoke test.
