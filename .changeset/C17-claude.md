---
"@horseness/adapter-claude": minor
---

Add the pinned Claude Code 2.1.228 native plugin adapter with ADR 0006 subscription-session authentication, one initial native MCP call carrying an exact batch of five capability references whose trusted runtime executes five complete real-authority `WorkerReturnV1` paths and returns every decision plus the accepted canonical advance, minimal tool-free real resume/fork marker invocations for durable SessionStart binding and context verification, crash-resumable kill-switch-first uninstall with atomic discovery disablement before authority revocation, minimal post-disable native initialization for uninstall discovery proof, no harness decision synthesis or monetary cap, fail-closed rate-limit/native-host handling, complete provenance, and a validated scenario-digest-bound subscription live receipt requiring `authMode: "existing-user-subscription-session"` without provider account or authentication data.
