---
"@horseness/adapter-claude": minor
---

Add the pinned Claude Code 2.1.228 native plugin adapter with ADR 0006 subscription-session authentication, one initial native MCP call carrying an exact batch of five capability references whose trusted runtime executes five complete real-authority `WorkerReturnV1` paths and returns every decision plus the accepted canonical advance, minimal tool-free real resume/fork marker invocations for durable SessionStart binding and context verification, and kill-switch-first uninstall. Discovery-disable recovery now reconciles the active path, verified disabled bytes, and exact inert sentinel idempotently, fsyncs every file and directory boundary, fails closed on ambiguity, missing evidence, symlinks, unexpected files, mode drift, or tamper, and proves real native inventory remains absent after injected crashes immediately following rename and sentinel fsync before continuing authority revocation.
