---
"@horseness/adapter-claude": minor
---

Add the pinned Claude Code 2.1.228 native plugin adapter with ADR 0006 subscription-session authentication, one initial native MCP call carrying an exact batch of five capability references whose trusted runtime executes five complete real-authority `WorkerReturnV1` paths and returns every decision plus the accepted canonical advance, minimal tool-free real resume/fork marker invocations for durable SessionStart binding and context verification, crash-resumable kill-switch-first uninstall whose restart recovery reads the durable configured discovery path and atomically disables it before any Claude process starts, repeated pinned-native `system/init` inventory checks against that same discovery reference before authority revocation and completion, no harness decision synthesis or monetary cap, fail-closed rate-limit/native-host handling, complete provenance, and a validated scenario matrix.
