---
"@horseness/installer": minor
"@horseness/cli": minor
"@horseness/bootstrap": minor
---

Add the bootstrap-pinned, release-manifest-authenticated neutral installer; real owner-only daemon bootstrap and opaque grant lifecycle; journaled staged upgrade, downgrade, rollback, crash retry, and compensation; pure packed doctor; repair/uninstall; offline dead-proxy blackbox; candidate-bound three-OS receipt workflow; and ten-command packed CLI lifecycle router.
